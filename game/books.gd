extends Node
class_name Books
# ★[S9-T7 / ADR-0067 결정 8 · ADR-0034 이행] 옥자 Books 채널 — 아이템 테이블 · 입수 롤 · 수집 원장.
#
# 목적: 옥자는 게임 중 호감도 미터가 없는 앵커라([ADR-0032]) 플레이어가 옥자를 **알아갈 능동
#       채널**이 비어 있다. [ADR-0034]가 지정한 그 채널이 이것이다 — 그날 밤 대화재로 흩어진
#       옥자의 것을 세계 곳곳에서 되찾으며 옥자·세계·조연 로어가 한 조각씩 드러난다.
#
# 두 형태(한 파이프라인 — ADR-0034 #1):
#   · **[옥자의 잃어버린 책들] 8권** = 답을 읽히는 무거운 한 챕터. 귀하고 **혼백관 전시 대상**이다.
#   · **[비밀 노트] 15장** = 질문을 흘리는 짧은 속삭임. 넓고 흔하며 **전시 대상이 아니다**
#     (떡밥은 유품이 아니다 — 전시대에 세울 물건이 아니라 주머니에 접어 넣는 종이다).
#
# 설계 메모:
#   - mailbox.gd와 **같은 결의 상태 노드**다: 단일 책임(수집 원장) + 얇은 to_save/load_save.
#     어디서 굴리는지·무엇을 대화창에 띄우는지는 전부 main이 조율하고, 이 노드는 "무엇을 주웠고
#     무엇을 읽었나"만 안다(DialogueBox·Inventory를 모른다).
#   - **텍스트는 전부 placeholder다(T8이 본문을 채운다).** 지금 여기 있는 것은 *그릇*이다 —
#     행을 고쳐 쓰는 것이 본문 반영의 전부이고 코드는 0줄 바뀐다(gift_prefs·LETTERS 규약 동형).
#   - ★ `lines`는 **평 Array**로 둔다(PackedStringArray 생성자는 const 표현식이 아니라 파스 에러).
#     변환은 `lines_of`가 한 자리에서 한다.
#   - ★ **메카닉 0**([ADR-0034] #4): 책은 영구 % 곱셈도, flat 범프도 주지 않는다. 보상은 *서사와
#     수집 자체*다. 이 파일에 XP·가격·배수 상수가 하나도 없는 것이 그 결정의 구조적 표현이다.
#     (저승 실용서 = XP 범프 트랙은 이 파일과 무관한 별 트랙이며 이번 슬라이스 밖이다.)
#   - ★ **선물 불가**(#7)·**비매**: 판정은 ItemCatalog(price 0)·GiftPrefs(CAT_BOOK 배제)가 지고
#     여기는 데이터만 든다. 되찾은 옥자의 유품 키프세이크라 남에게 건네는 것이 톤 위반이다.

# ── 텍스트 테이블 ────────────────────────────────────────────────────────────
# id → { "title": 표시명(인벤·대화창 이름판), "lines": 본문 줄들(평 Array), "note": 작성 근거 메모 }
#
# ★[S9-T8] 본문 주입 완료 — placeholder는 전부 실제 로어로 대체됐다(코드 0줄 변경).
#
# **작성 규약(전 23텍스트 공통 — [ADR-0067] 결정 8 봉인 법칙 체크리스트):**
#   ① **증거·공명만.** 평결을 말하지 않는다 — 무엇이 있었는지까지만 적고 그것이 무슨 뜻인지는
#      안 정한다. 그래서 거의 모든 행이 *기록체*(장부·처방·명부 사본·유래 정리)다.
#   ② **플레이어 죄목의 중심 평결 서술 0.** "네 죄는 외면·부재"는 물론이고, 옥자의 희생·기억
#      봉인의 *사실 진술*도 안 한다. 대신 그 자리에 **빈칸의 흔적**을 둔다(명부의 먹으로 덮인
#      사유란·지운 획·끊긴 기록) — 빈칸은 증거지 평결이 아니다.
#   ③ **세 조각(감정·사실·행동) 서술 0.** 그 셋은 미호·멜·바나 독점이라, 책은 옥자가 밤마다
#      우는 것도·숨긴 장부도·잠긴 문의 범인도 말하지 않는다. 노트 ⑩의 걸쇠 모순처럼 **행위자
#      없는 물증**까지만 둔다.
#   ④ **잇는 여지.** 전 텍스트가 *연결되지 않은 채로* 끝난다. 옥자의 환자 기록(책 ⑦)과 명부의
#      빈 한 칸(책 ⑤)을 잇는 것은 플레이어의 몫이고, 어떤 행도 그 선을 대신 그어 주지 않는다.
#   ⑤ **재읽힘 이중성.** 여러 행이 두 번째 읽을 때 다르게 읽히도록 쓰였다(책 ②의 "물밑",
#      책 ⑥의 아득함 두 뜻, 책 ⑧의 "먹일 사람이 하나뿐"). **분기 코드는 없다** — 문장 자체가
#      두 겹이라 [해방] 후 재읽기에서 층이 열린다([ADR-0034] #3 "환경적 공명").
#   ⑥ **볼륨**([ADR-0067] 결정 12): 각 텍스트 3~8줄. 실제 = **책 7줄 · 노트 3줄**로 두어
#      "책=무거운 한 챕터 / 노트=짧은 속삭임"의 위계가 *길이로도* 읽히게 했다.
# books_test ②h(볼륨·위계)와 ⑨a(금칙어 스캔)가 이 규약을 회귀로 잠근다.
const BOOK_PREFIX := "book_okja_"
const NOTE_PREFIX := "note_secret_"

const BOOK_COUNT := 8    # [옥자의 잃어버린 책들] 권수(ADR-0067 결정 8 볼륨 — 잠정·owner 큐)
const NOTE_COUNT := 15   # [비밀 노트] 장수(같은 잠정)

# 되찾은 책 — 무거운 한 챕터. 전시 대상(혼백관 좌대 8좌).
const BOOKS := {
	"book_okja_1": {
		"title": "옥자의 잃어버린 책 ① — 약재기",
		"lines": [
			"그을린 표지의 책이다. 페이지가 서로 눌어붙어 있다.",
			"「액운은 태워 없애는 것이 아니라 옮겨 담는 것이다. 담을 그릇이 있어야 한다.」",
			"「그릇은 대개 사람이다. 그래서 이 일은 값이 비싸고, 나는 값을 잘 못 받는다.」",
			"「삼킨 것은 사흘이면 가라앉고 이레면 내 것이 된다. 그때부턴 아프지 않다.」",
			"「아프지 않은 것과 나은 것은 다르다. 이 구분을 나는 자꾸 잊는다.」",
			"뒷장에는 마지막 문장만 열 번 넘게 다시 적혀 있다. 획이 갈수록 깊다.",
			"「— 옥자」",
		],
		"note": "옥자 인물/약사 결 — 남의 액운을 제 몸에 옮겨 담던 방식(CONTEXT 옥자·바이블 §5.1). "
			+ "'가라앉힌다'는 어휘를 여기서 먼저 깔아 책 ②의 세계 규칙과 공명시킨다(평결 없음)",
	},
	"book_okja_2": {
		"title": "옥자의 잃어버린 책 ② — 마녀의 규범",
		"lines": [
			"물에 한 번 젖었다 마른 책이다. 종이가 우글쭈글하다.",
			"「나라카의 규칙 몇 가지를 적어 둔다. 잊으면 사람이 상한다.」",
			"「하나. 묶는 것은 이름이다. 이름을 모르면 못 묶고, 이름을 부르면 반드시 묶인다.」",
			"「둘. 가라앉힌 것은 없앤 것이 아니다. 물밑에 그대로 있고 무게도 그대로다.」",
			"「셋. 물밑의 것은 남이 건져 줄 수 없다. 대신 손을 넣으면 더 깊이 밀려난다.」",
			"「그래서 마녀가 할 수 있는 일은 물가에 서서 기다리는 것뿐이다. 이것이 제일 어렵다.」",
			"「— 옥자」",
		],
		"note": "세계 규칙 결 — 봉인 법칙([ADR-0016] 키스톤)을 *세계 물리*로만 적는다. "
			+ "누구의 봉인인지·무엇이 가라앉았는지는 안 적으므로 평결이 아니라 규칙이다. "
			+ "★ 이 책 자체가 '책은 답을 대신 말해 주지 못한다'는 선언(체크리스트 ④의 자기 진술)",
	},
	"book_okja_3": {
		"title": "옥자의 잃어버린 책 ③ — 물길과 자리",
		"lines": [
			"표지가 뜯겨 나가고 속장만 남은 책이다.",
			"「나루는 배가 닿는 자리라 마을이 되었다. 모든 길이 그 자리를 지난다.」",
			"「혼백관은 배에서 내린 사람이 손에 쥐고 온 것을 내려놓는 자리다. 대개 값이 없는 물건이고, "
				+ "값 없는 것을 끝까지 쥐고 오는 사람만 온다.」",
			"「삼도천은 하구로 갈수록 넓어져 황천해가 된다. 넓어질수록 건너온 까닭은 흐려진다.」",
			"「미혹의 숲은 길이 없어 길을 잃는 곳이 아니다. 찾는 것이 분명하지 않으면 길이 안 열린다.」",
			"「그래서 나는 그 끝에 집을 지었다. 아무나 오지 않기를 바랐고, 그 바람은 잘 지켜지고 있다.」",
			"「— 옥자」",
		],
		"note": "지리/역사 결 — 나루·혼백관·삼도천·황천해·미혹의 숲 유래(CONTEXT 지리). "
			+ "마지막 두 줄이 '옥자 집은 찾는 자에게만 열린다'는 지리-서사 정합(바이블 §6.5-1)의 "
			+ "인-픽션 근거이자, 되찾는 행위 자체에 대한 공명",
	},
	"book_okja_4": {
		"title": "옥자의 잃어버린 책 ④ — 손님장",
		"lines": [
			"실로 꿰맨 자국이 여러 번 덧대어진 책이다.",
			"「약방에 드나든 것들을 적어 둔다. 값은 대개 못 받았다.」",
			"「뒷마당에 쓰러져 있던 큰 것. 무서워하지 않았더니 사흘째부터 장작을 팼다. 손이 크고 조심스럽다.」",
			"「메밀묵을 놓아 두면 없어진다. 그릇은 늘 씻어서 돌려 놓는다. 장난은 치는데 예의는 있다.」",
			"「열이 안 잡히는 밤에는 산에서 찬 것이 내려와 문가에 앉아 있다 간다. 값을 물으면 화를 낸다.」",
			"「심해 약초는 우물가로 온다. 노랫소리가 먼저 오고 물건이 나중에 온다.」",
			"「깊은 산의 것은 오지 않는다. 내가 그 아이 저주를 지웠으니 원한을 살 만도 하다.」",
			"「— 옥자」",
		],
		"note": "조연 사연 결 — 켄(뒷마당 거인)·깨비(메밀묵)·설화(냉기 조력)·세레나(심해 약초·"
			+ "우물)·루카(저주 지움)의 *약방 시절 흔적*만(바이블 §5.2~5.3). 이름을 하나도 안 적어 "
			+ "'이게 누구지'가 남는다 — 조연 ♡3 고백의 몫을 뺏지 않는다(코러스 모델 불가침)",
	},
	"book_okja_5": {
		"title": "옥자의 잃어버린 책 ⑤ — 그 밤의 사본",
		"lines": [
			"모서리가 새까맣게 탄 책이다. 가운데는 멀쩡하다.",
			"「그 밤의 것은 남이 적은 대로만 옮겨 둔다. 내 말은 안 적는다.」",
			"「명부 사본: 그 자리에서 거둔 혼 다섯. 기재된 것은 넷. 한 칸이 비어 있고 사유란은 먹으로 덮였다.」",
			"「같은 밤, 차사 하나가 직을 잃고 이곳으로 내려왔다. 사유는 월권. 그 뒤는 안 적혀 있다.」",
			"「불은 기름에서 시작했다고 되어 있으나, 번진 속도가 기름의 것이 아니다. 두 가지 불이 겹쳤을 때의 속도다.」",
			"「우물이 하나 통째로 사라졌다. 물이 마른 것이 아니라 자리가 없어졌다.」",
			"「이 사본을 가지고 있어도 되는 사람이 아닌데, 나는 가지고 있는다.」",
		],
		"note": "그날 밤의 **세계측** 조각 — 전부 남이 적은 기록의 전재(轉載)다: 명부 결번·차사 "
			+ "유배·불의 이상 확산·사라진 우물(바이블 §3). ★ 핵심은 **빈 한 칸**이다 — 누가 비었는지 "
			+ "안 적으므로 증거이지 평결이 아니고, 책 ⑦의 환자 기록과 잇는 선은 플레이어 몫으로 남는다",
	},
	"book_okja_6": {
		"title": "옥자의 잃어버린 책 ⑥ — 절기 유래",
		"lines": [
			"낡은 끈으로 묶인 책이다. 매듭이 단단하다.",
			"「절기 이름의 유래를 정리한다. 셋은 쉬웠고 하나는 두 번 고쳐 적었다.」",
			"「피안절 — 저편 기슭. 유화절 — 흐르는 불. 성야절 — 길고 잿빛인 별밤.」",
			"「망연절 — 아득함. 아득하다는 말은 멀다는 뜻도 되고, 분명 있었는데 안 보인다는 뜻도 된다.」",
			"글자 아래 지운 자국이 있다. 같은 자리에 두 번째 글자를 다르게 쓴 획이 눌려 남아 있다.",
			"「이렇게 읽는 사람은 없을 것이다. 없어도 이름은 이름이다.」",
			"「이 절기가 오면 나는 하루쯤 밖에 나가지 않는다. 까닭은 안 적는다.」",
		],
		"note": "환경적 공명 — 망연절 茫然→忘緣 이중 한자([CONTEXT] 저승 절기)를 **밝히지 않고 "
			+ "지운 획의 물증으로만** 둔다. 진명(真名) 공개는 클라이맥스 몫이므로 여기서 읽어 주면 "
			+ "봉인 법칙 위반이다. 두 번째 독해에서 '아득함'의 두 뜻이 갈라지는 대표 재읽힘 층",
	},
	"book_okja_7": {
		"title": "옥자의 잃어버린 책 ⑦ — 환자 기록",
		"lines": [
			"먼지가 두껍게 앉은 책이다. 손자국이 하나 남아 있다.",
			"「환자 기록. 탁기(濁氣). 산 것에게 물렸고, 물린 자리보다 안쪽이 먼저 상했다.」",
			"「초이레: 약이 안 듣는다. 내 피를 섞어 본다. 듣는다.」",
			"「스무이레: 내 쪽이 줄어드는 속도가 저쪽이 나아지는 속도보다 빠르다. 계산은 맞다. 그만둘 까닭은 못 찾았다.」",
			"「백일: 걸어서 정원까지 나왔다. 오늘은 아무것도 기록하고 싶지 않다.」",
			"기록은 여기서 끊기고, 마지막 장에는 약재 이름 대신 한 줄만 남아 있다.",
			"「이 사람이 오래 사는 쪽으로 계산을 다시 한다.」",
		],
		"note": "옥자 생전 — 탁기 환자를 제 피로 살린 치료 일지(바이블 §2·§5.1). 환자가 **누구인지 "
			+ "한 번도 안 적는다**(이름·호칭 0). 옥자의 생전은 풀어도 되는 것이므로 증거는 다 주되, "
			+ "'그게 나였다'를 잇는 것은 플레이어 몫 — 타인의 말로 봉인이 풀리지 않는다",
	},
	# ★ ⑧은 [ADR-0034] #6의 「옥자의 잃어버린 레시피」 자리다 — 히든 융합 메뉴 1종의 해금 표적.
	#   **해금 배선 자체는 이번 슬라이스 밖**이고(발견 게이트 = ADR-0064 소관), 여기는 그 자리가
	#   실존한다는 것까지만 한다(나락 열쇠가 아이템으로 먼저 등록되고 점등은 다음 태스크가 한
	#   그 선례 동형).
	"book_okja_8": {
		"title": "옥자의 잃어버린 책 ⑧ — 조리 기록",
		"lines": [
			"약재 이름과 계량이 빼곡한 공책이다. 요리책 같기도 하다.",
			"「약은 쓰면 안 먹는다. 먹어야 낫는데 쓰면 안 먹으니, 결국 맛이 약의 절반이다.」",
			"「혼령초 두 줌 — 데치지 말고 살짝 그을릴 것. 쓴맛이 향으로 바뀐다.」",
			"「피안화 한 송이 — 붉은 물이 다 빠질 때까지. 색이 남으면 잠이 안 온다.」",
			"「혼백도 반 개 — 맨 마지막에. 단맛은 약을 삼키게 하는 손이지 약이 아니다.」",
			"「식힌 뒤 잔에 붓고 여우불 한 점만큼의 온기만 얹는다. 뜨거우면 향이 죽는다.」",
			"「이름은 안 붙였다. 먹일 사람이 하나뿐이라 이름이 필요 없었다.」",
		],
		"note": "[ADR-0034] #6 히든 융합 레시피 — 약재 계량이 그대로 조리법으로 읽히는 이중성"
			+ "(혼령초·피안화·혼백도 = 실재 [저승 작물]/[혼의 나무] 산출이라 재료가 인게임에 있다). "
			+ "★ **해금 배선은 이번 슬라이스 밖**이라 이 행은 여전히 본문뿐이고, 마지막 줄이 "
			+ "이름 없는 메뉴에 이름을 붙일 자리를 비워 둔다(평결 0 — 먹일 사람이 누구인지는 안 적는다)",
	},
}

# 비밀 노트 — 짧은 속삭임. 세계 전체의 텍스처이고 **전시 대상이 아니다**.
#
# ★[S9-T8] **노트는 답을 안 준다 — 물음표만 남기고 끝난다.** 그래서 열다섯 장 전부가
#   ㉠ 물성 한 줄 → ㉡ 남이 적은 한두 줄(대개 의문문·모순의 지적)의 3줄 구조이고, 서명이 없다
#   (누가 썼는지 모르는 것이 노트의 정체성 — 옥자 유품인 책과 갈리는 지점).
# ★ **옥자 브랜드로 좁히지 않는다**([ADR-0034] 대안 B 기각 근거): 절반 이상이 옥자와 무관한
#   세계 텍스처(나루의 관습·율법·설산·전령)이고, 그날 밤에 닿는 것들도 *행위자 없는 물증*까지다.
const NOTES := {
	"note_secret_1": {"title": "비밀 노트 ①", "lines": [
		"접힌 자국이 깊은 쪽지다.",
		"「배에서 내릴 때 쥔 걸 놓으라고들 한다.」",
		"「놓고 나면 왜 손이 더 시릴까.」",
	], "note": "나루 관습 — 혼백관 유품 정서의 세계측 텍스처(책 ③과 공명·평결 0)"},
	"note_secret_2": {"title": "비밀 노트 ②", "lines": [
		"귀퉁이가 찢긴 쪽지다.",
		"「명부의 결번은 실수로는 안 생긴다고 했다.」",
		"「그럼 누가 만드는 걸까.」",
	], "note": "책 ⑤의 빈 한 칸을 *질문으로만* 되받는다 — 답은 어디에도 안 적힌다"},
	"note_secret_3": {"title": "비밀 노트 ③", "lines": [
		"누군가 급히 적은 듯한 쪽지다.",
		"「그 밤에 불이 두 번 켜졌다.」",
		"「한 번은 파랬고 한 번은 파랗지 않았다.」",
	], "note": "그날 밤 세계측 물증(여우불+도깨비불 공명) — 목격 색깔까지만, 누구인지는 0"},
	"note_secret_4": {"title": "비밀 노트 ④", "lines": [
		"뒷면에 숫자만 몇 개 적힌 쪽지다.",
		"「합계가 안 맞는다. 한 줄이 빠져 있다.」",
		"「빠진 줄이 제일 크다.」",
	], "note": "장부 결의 텍스처 — 멜의 *사실의 조각*(숨긴 장부)은 안 건드리고 산술 모순만 둔다"},
	"note_secret_5": {"title": "비밀 노트 ⑤", "lines": [
		"물기에 눌린 자국이 남은 쪽지다.",
		"「우물 안에서 노래를 들었다는 사람이 셋이다.」",
		"「그 우물을 봤다는 사람은 없다.」",
	], "note": "사라진 우물·세레나 흔적(바이블 §3) — 증인은 있고 물건은 없는 구조"},
	"note_secret_6": {"title": "비밀 노트 ⑥", "lines": [
		"재가 묻은 쪽지다.",
		"「장난은 불씨가 아니라고 했다.」",
		"「그럼 불씨는 어디서 왔나.」",
	], "note": "깨비 파편의 바깥 그림자 — 조연 ♡3 고백의 자리를 비워 두고 물음만"},
	"note_secret_7": {"title": "비밀 노트 ⑦", "lines": [
		"실로 묶였던 구멍이 남은 쪽지다.",
		"「이 처방은 두 사람 몫이다.」",
		"「한 사람은 마시고 한 사람은 덜어 낸다.」",
	], "note": "책 ①·⑦의 '옮겨 담기'를 처방전 한 장으로 되비춘다(누구인지 0)"},
	"note_secret_8": {"title": "비밀 노트 ⑧", "lines": [
		"반듯하게 잘린 쪽지다.",
		"「율법: 산 자와 죽은 자를 바꾸어 적지 말 것.」",
		"아래에 다른 글씨로 한 줄이 덧붙어 있다. 「한 번은 있었다.」",
	], "note": "세계 규칙 + 그 규칙이 깨진 흔적. 언제·누구는 안 적혀 잇는 여지가 남는다"},
	"note_secret_9": {"title": "비밀 노트 ⑨", "lines": [
		"봉투 안쪽을 뜯어 쓴 쪽지다.",
		"「부칠 데가 없는 편지는 어디로 가나.」",
		"「전령한테 물었더니 웃기만 했다.」",
	], "note": "[저승 전령] 우편 채널(T3)의 세계 텍스처 — 순수 분위기, 그날 밤 무관"},
	"note_secret_10": {"title": "비밀 노트 ⑩", "lines": [
		"가장자리가 눌어붙은 쪽지다.",
		"「문은 안에서 잠겼다고 적혀 있다.」",
		"「걸쇠는 바깥에 달린 문이었다.」",
	], "note": "★ 행위자 없는 물증의 표본 — 바나의 *행동의 조각*(자백)은 건드리지 않고 "
		+ "기록과 물건의 모순만 남긴다. 범인도 결론도 안 적는다"},
	"note_secret_11": {"title": "비밀 노트 ⑪", "lines": [
		"두 번 접어 넣어 둔 쪽지다.",
		"「망연절엔 다들 뭔가를 깜빡한다.」",
		"「깜빡했다는 것만 기억나고 무엇을 깜빡했는지는 모른다.」",
	], "note": "책 ⑥ 절기 유래의 짧은 짝 — 재읽힘 층(두 번째 독해에서 절기 이름과 겹친다)"},
	"note_secret_12": {"title": "비밀 노트 ⑫", "lines": [
		"잉크가 한쪽으로 쏠린 쪽지다.",
		"「이름을 부르면 묶인다면,」",
		"「이름을 잊은 건 풀린 걸까 잃은 걸까.」",
	], "note": "책 ② 규범 하나를 질문으로 뒤집는다 — 순수 세계 규칙 사유(대상 지목 0)"},
	"note_secret_13": {"title": "비밀 노트 ⑬", "lines": [
		"손바닥만 한 얇은 쪽지다.",
		"「설산에 큰 게 산다는데 아무도 무섭다고는 안 한다.」",
		"「다들 슬프다고 한다.」",
	], "note": "프로스티의 세계측 소문 — 정체(공허의 형상화)는 그 캐릭터 몫이라 인상만"},
	"note_secret_14": {"title": "비밀 노트 ⑭", "lines": [
		"말려 있던 자국이 그대로 남은 쪽지다.",
		"「그 밤에 뭔가 두 개가 태어났다는 말이 있다.」",
		"「태어난 것들은 자기 생일을 모른다.」",
	], "note": "모찌·네오 탄생의 소문층 — 기원(눈물·명부 파편)은 비인간 ♡3 비밀이라 안 적는다"},
	"note_secret_15": {"title": "비밀 노트 ⑮", "lines": [
		"끝줄만 유난히 진하게 눌러 쓴 쪽지다.",
		"「이런 걸 모으고 있다는 건 찾고 있다는 뜻이다.」",
		"「찾는 것이 분명한 사람에게만 길이 열린다고 했다.」",
	], "note": "수집 행위 자체에 대한 공명(책 ③ 미혹의 숲 규칙의 반향). 마지막 한 장이 "
		+ "'답은 여기 없다'를 스스로 말해 봉인 법칙 ④를 텍스트 안에서 닫는다"},
}

# ── 입수 원천(플레이 부산물 — ADR-0067 결정 8) ────────────────────────────────
# **한 활동에 몰지 않는 이유**: 어떤 플레이 스타일이든 로어를 만나게 하기 위해서다. 농사만
# 짓는 사람도, 갱도만 파는 사람도, 낚시만 하는 사람도 같은 이야기에 닿아야 한다.
const SRC_MINE := "mine"        # 채굴 — 광맥/돌을 부순 사건마다(★[폴리시 R4] 갱도·나락 두 무대 공통)
const SRC_FISH := "fish"        # 낚시 — 포획 성공마다(인양 롤 다음 가지)
const SRC_FORAGE := "forage"    # 미혹의 숲 희소 채집 — 미혹 구역 채집 줍기마다
const SRC_DEBRIS := "debris"    # 농장 debris — 개간 한 칸마다

# 원천별 드랍 확률(퍼밀 · 0..999 롤 대비). **잠정값 — owner 큐**(ADR-0067 결정 12 결).
# 밴드 근거는 *활동 빈도의 역수*다: 한 판에 수십 번 굴리는 채굴·개간은 낮게, 한 번이 무거운
# 낚시 포획·미혹 희소 채집은 높게 둬 **어느 축으로 놀아도 비슷한 속도로 만나게** 한다.
#   · 채광 15퍼밀(1.5%/부순 돌) — 층 하나에 돌이 수십이라 실효 조우가 가장 잦은 축
#   · 낚시 40퍼밀(4%/포획)     — 인양 롤(30퍼밀)과 같은 밴드. 낚시 한 판 = 몇 마리다
#   · 채집 70퍼밀(7%/줍기)     — 미혹 희소 채집은 하루 몇 자리뿐이라 가장 높다
#   · 개간 10퍼밀(1%/한 칸)    — 초반 개간은 수백 칸이라 가장 낮다
const DROP_PERMIL := {
	SRC_MINE: 15,
	SRC_FISH: 40,
	SRC_FORAGE: 70,
	SRC_DEBRIS: 10,
}

# 드랍이 걸린 뒤 **무엇이 나오나** — 노트가 흔하고 책이 귀하다(ADR-0034 #1 "노트=넓음·흔함 /
# 책=귀함"의 수치 표현). 700퍼밀 이상이면 책, 미만이면 노트.
const BOOK_SHARE_PERMIL := 700

# ── 원장(세이브 대상) ─────────────────────────────────────────────────────────
var acquired: Dictionary = {}     # id → 주운 day(int). 있으면 = 이미 세상에서 나왔다(중복 입수 0)
var read_ledger: Dictionary = {}  # id → true(읽음). 주운 순간 즉독이 보통이지만, 대화 중 주우면 미독으로 남는다

# ★ 책장 재읽기 커서는 **세이브하지 않는다**(휘발). 진행이 아니라 "지금 어디까지 넘겨 봤나"라
#   이어하기가 기억할 값이 아니다 — 이어하면 처음부터 다시 넘긴다(_talking_to와 같은 결).
var _shelf_cursor: int = 0

signal changed()   # 입수·열람·복원 프레임(main이 배지·전시·프롬프트를 다시 그린다)

# ── 테이블 조회(정적 — 어디서든 Books.title_of(id)) ───────────────────────────
static func is_book(id: String) -> bool:
	return BOOKS.has(id)

static func is_note(id: String) -> bool:
	return NOTES.has(id)

# 이 id가 CAT_BOOK 아이템인가(책 ∪ 노트). ItemCatalog._is_book이 이 한 술어에 위임한다.
static func has_text(id: String) -> bool:
	return BOOKS.has(id) or NOTES.has(id)

static func _row(id: String) -> Dictionary:
	if BOOKS.has(id):
		return BOOKS[id]
	if NOTES.has(id):
		return NOTES[id]
	return {}

static func title_of(id: String) -> String:
	return str(_row(id).get("title", ""))

# 본문 줄들(평 Array → PackedStringArray 변환은 여기 한 자리에서).
static func lines_of(id: String) -> PackedStringArray:
	var out := PackedStringArray()
	var raw: Variant = _row(id).get("lines", [])
	var t := typeof(raw)
	if t != TYPE_ARRAY and t != TYPE_PACKED_STRING_ARRAY:
		return out
	for ln in raw:
		out.append(str(ln))
	return out

static func book_ids() -> Array:
	return BOOKS.keys()

static func note_ids() -> Array:
	return NOTES.keys()

# 전 id(책 먼저·선언 순). ItemCatalog.ids_in_category·수집 트래커 분모가 쓴다.
static func all_ids() -> Array:
	var out: Array = BOOKS.keys()
	out.append_array(NOTES.keys())
	return out

# ── 입수 롤(순수 — 결정적) ────────────────────────────────────────────────────
# ★ 프로젝트 관례: `hash("<네임스페이스>:<day>:<serial>")` → **`rand_from_seed` 믹싱** → % N.
#   해시값을 바로 %로 접으면 djb2 하위 비트 편향으로 특정 분위가 통째로 비는 사고가 있었다
#   (weather.gd 헤더의 그 실증 — S7). 네임스페이스가 원천마다 달라 네 축이 서로 안 얽힌다.
static func _roll(ns: String, day: int, serial: int, modulo: int) -> int:
	return absi(rand_from_seed(hash("%s:%d:%d" % [ns, day, serial]))[0]) % maxi(modulo, 1)

# 이 원천의 드랍 확률(퍼밀). 모르는 원천은 0 = 절대 안 나옴(오타 방어).
static func permil_of(source: String) -> int:
	return int(DROP_PERMIL.get(source, 0))

# **무엇이 나오는가**(순수 함수 — 원장을 인자로 받는다). "" = 이번엔 없음.
#   ㉠ 게이트 롤: 원천별 퍼밀 미만이면 통과
#   ㉡ 종류 롤: 책이냐 노트냐(BOOK_SHARE_PERMIL)
#   ㉢ 지목 롤: **아직 안 주운 것들 중에서만** 뽑는다(중복 입수 0 — 같은 책이 두 번 나오면
#      수집이 아니라 버그로 읽힌다. mailbox.send의 멱등과 같은 판단)
#   ㉣ 고른 종류가 이미 동나면 **다른 종류로 넘어간다**(막힘 0 — 책을 다 모은 사람에게도
#      노트는 계속 나오고, 그 반대도 같다). 둘 다 동나면 "".
static func peek_drop(source: String, day: int, serial: int, owned: Dictionary) -> String:
	var permil := permil_of(source)
	if permil <= 0:
		return ""
	if _roll("book:" + source, day, serial, 1000) >= permil:
		return ""
	var want_book := _roll("bookkind:" + source, day, serial, 1000) >= BOOK_SHARE_PERMIL
	var pool := _remaining(BOOKS.keys() if want_book else NOTES.keys(), owned)
	if pool.is_empty():
		pool = _remaining(NOTES.keys() if want_book else BOOKS.keys(), owned)
	if pool.is_empty():
		return ""
	return str(pool[_roll("bookpick:" + source, day, serial, pool.size())])

# 아직 안 주운 id만 추린다(선언 순 보존 — 뽑기 인덱스가 결정적이려면 순서가 안정해야 한다).
static func _remaining(ids: Array, owned: Dictionary) -> Array:
	var out: Array = []
	for id in ids:
		if not owned.has(id):
			out.append(id)
	return out

# 인스턴스 창구 — 지금 원장으로 이번 롤의 결과를 본다(적재는 아직 안 한다).
# ★ **지목과 적재를 가른** 이유: 인벤이 가득이면 주울 수 없는데, 원장에 먼저 적으면 그 책이
#   세상에서 영영 사라진다(수집 완주 불가 = 치명). main은 add_item이 성공한 뒤에만 acquire한다.
func pending_drop(source: String, day: int, serial: int) -> String:
	return peek_drop(source, day, serial, acquired)

# ── 원장 ──────────────────────────────────────────────────────────────────────
# 주웠다고 적는다. 이미 있는 id·테이블에 없는 id는 false(멱등).
func acquire(id: String, day: int) -> bool:
	if not has_text(id) or acquired.has(id):
		return false
	acquired[id] = day
	changed.emit()
	return true

func has_acquired(id: String) -> bool:
	return acquired.has(id)

func acquired_count() -> int:
	return acquired.size()

# 주운 책 수 / 노트 수(수집 트래커 표기 — 두 형태를 따로 센다).
func acquired_book_count() -> int:
	var n := 0
	for id in acquired:
		if is_book(str(id)):
			n += 1
	return n

func acquired_note_count() -> int:
	return acquired_count() - acquired_book_count()

# 읽음 처리. 주운 적 없는 id는 무시한다(늦은 입력·손상 호출 방어). 이미 읽었으면 조용히 false.
func mark_read(id: String) -> bool:
	if not acquired.has(id) or read_ledger.has(id):
		return false
	read_ledger[id] = true
	changed.emit()
	return true

func is_read(id: String) -> bool:
	return read_ledger.has(id)

func unread_count() -> int:
	var n := 0
	for id in acquired:
		if not read_ledger.has(str(id)):
			n += 1
	return n

# 주운 순서대로 나열한다(day 오름차순 → 같은 날이면 선언 순). 책장 재열람·트래커가 쓴다.
func acquired_ids() -> Array:
	var out: Array = []
	for id in all_ids():
		if acquired.has(id):
			out.append(id)
	out.sort_custom(func(a, b): return int(acquired[a]) < int(acquired[b]))
	return out

# ── 책장 재읽기(HOME 집 책장 [F]) ─────────────────────────────────────────────
# **미독이 있으면 미독을 먼저**(대화 중에 주워 놓친 것이 여기서 회수된다), 전부 읽었으면 커서를
# 한 칸씩 돌리며 차례로 다시 펼친다(연타 = 다음 권). 주운 것이 없으면 "".
# ★ 즉독(주운 자리)과 **같은 데이터 위**에 서는 두 번째 창구일 뿐이다 — 별도 상태가 없다.
func next_reread() -> String:
	var ids := acquired_ids()
	if ids.is_empty():
		return ""
	for id in ids:
		if not read_ledger.has(id):
			return str(id)
	var pick := str(ids[posmod(_shelf_cursor, ids.size())])
	_shelf_cursor = posmod(_shelf_cursor + 1, ids.size())
	return pick

# ── 세이브/로드 ───────────────────────────────────────────────────────────────
# 가법 키 두 축(입수 원장 · 기독 원장). ★ 키 자체가 없는 **구세이브는 main이 load_save를 아예
# 안 불러 빈 원장으로 시작**한다(우편함·곳간·상자가 쓰는 그 하위호환 관례 — 마이그레이션 0).
func to_save() -> Dictionary:
	return {"acquired": acquired.duplicate(), "read": read_ledger.duplicate()}

# 복원 = 정제하며 갈아끼운다(mailbox.load_save의 결 그대로):
#   · 테이블에서 사라진 옛 id는 조용히 버린다(본문 없는 책은 펼칠 수 없어 빈 대화창 함정이 된다)
#   · 기독 원장은 **입수 원장에 있는 id만** 든다(주운 적 없는 것의 기독 기록은 의미가 없고,
#     남겨 두면 나중에 실제로 주웠을 때 미독 표시 없이 묻힌다)
func load_save(data: Dictionary) -> void:
	acquired = {}
	var raw_acq: Variant = data.get("acquired", {})
	if typeof(raw_acq) == TYPE_DICTIONARY:
		for k in raw_acq:
			var sid := str(k)
			if has_text(sid):
				acquired[sid] = int(raw_acq[k])
	read_ledger = {}
	var raw_read: Variant = data.get("read", {})
	if typeof(raw_read) == TYPE_DICTIONARY:
		for k in raw_read:
			var sid2 := str(k)
			if acquired.has(sid2) and bool(raw_read[k]):
				read_ledger[sid2] = true
	_shelf_cursor = 0
	changed.emit()
