# 안식농원 맵 종합 감사 — 스타듀밸리 대조 (owner 2026-07-17 라이브)

> **요청:** "안식농원 전체를 스타듀밸리 맵과 비교해서, 맵을 많이 바꾸게 되더라도 어떤 부분을 개선할지 찾아줘."
> **근거:** 라이브 전체 덤프(`tools/home_full_dump.png`, 80×65, dissolve 반영) + [`stardew-terrain-taxonomy.md`](./stardew-terrain-taxonomy.md) + 스타듀 위키 Standard Farm.
> **핵심 결론:** 문제는 낱개 타일이 아니라 **① 균일 tan 베이스(단조) ② 전 피처의 하드 직선/직사각 경계 ③ "놓인" 느낌(자연 발생 아님)** 세 층위. ①이 근본 결정(ADR-0053)과 얽혀 있어 **crux 결정**이 먼저 필요.

---

## 0. 라이브 전체 맵에서 실제 보이는 것

균일한 tan 평원 전면 + 그 위에 흩뿌린 오브젝트(풀tuft·twig·stone·잡초). 피처들:
- **길(동선):** 하드 엣지 갈색 직선 스트립(수직 중앙 + 수평 1줄). `_build_path_grass_fringe`가 있으나 tan 위에선 물결이 안 읽힘.
- **연못:** 정사각 파란 박스 + 상단만 널판 테두리. 하드 직각.
- **절벽/고지:** 좌측 2개의 직사각 갈색 "구덩이"(grass top + dirt face). 구멍처럼 읽힘.
- **덤불 벽:** 완벽한 일렬 세로 덤불 12개(SOLID 경계). 극도로 인공적.
- **건물 발치:** 하드 직사각 tan 패드.
- **밭(SOIL):** 낱개 둥근 사각 타일이 격자 간격으로 뚝뚝(오토타일 미연결 — owner "따로따로 생성").

## 1. Crux — 베이스 지형: tan-단조 (가장 큰 격차, 결정 필요)

**스타듀 팜 = 잔디(초록) 지배 + 흙 패치.** 안식 = **흙(tan) 지배 + 잔디 스캐터**([ADR-0053] 봉인). 전체 덤프로 보면 이 선택이 맵을 **황무지/사막**처럼 만든다 — 스타듀의 lush farm과 정반대. owner의 반복된 "잔디가 거의 없다"가 사실 이 근본이다.

**긴장:** [ADR-0053]은 "overgrown 개간 황무지를 복원한다"는 속죄/개간 테마([ADR-0035/0055])로 흙-지배를 *의도적으로* 봉인했다. 그런데 스타듀 시작 팜도 잡초·돌 무성하지만 **베이스는 초록 잔디**다("overgrown"이 곧 "tan"은 아니다). 즉 "개간 전 방치"를 tan 사막으로 표현한 게 과했을 수 있다.

**crux 결정 — RESOLVED (owner 2026-07-17, 레퍼런스 `Desktop/naraka/map/스크린샷 2026-07-06 오후 9.40.44.png` = 스타듀 Forest Farm 지목):**

> **확정 = B/C(흙-지배 유지 + 유기 잔디뭉치 + 나무/절벽 경계).** 레퍼런스가 정확히: **① base=tan/흙 지배(ADR-0053 유효·A안 lush 피벗 기각) ② 흙 위에 채워진 잔디 "뭉치(패치)"가 불규칙 clump으로 분포(낱개 tuft 아님) ③ 사이드=전부 빽빽한 나무 + 바위 절벽 ④ 물=유기형 blobby 연못 ⑤ 나무 내부에도 밀집 ⑥ 돌·그루터기·잡초 overgrown 스캐터 유지.**
>
> **★정정:** 앞서(같은 세션) taxonomy를 Standard Farm 기준으로 읽어 "tuft-only·잔디패치 폐지"로 갔던 것은 과교정. Forest Farm 레퍼런스는 **잔디 패치(채움 clump)가 명백히 존재** → `_G16_GRASS_PATCHES` 재활성(유기적으로). 이전 패치 폐지 사유(잔디↔흙 하드 Wang 경계)는 **소프트 tuft 프린지 엣지**로 해소(레퍼런스도 경계가 tuft로 풀림 = owner "1자 경계" 직결).
>
> **슬라이스:** ①유기 잔디뭉치 부활(CA 마스크→`_bf_grass` 패치+소프트 엣지) ②경계=나무선+절벽(일렬 덤불 폐기) ③물 유기형 ④밭 오토타일 ⑤건물 접지 ⑥내부 나무 밀도. CA 클럼프 마스크(scatter 브랜치 Task 3)가 ①의 엔진.

## 2. 감사 — 우선순위별 개선 항목

### P1 · 하드 직선/직사각 경계 전면 (owner "1자 경계" 직격)
스타듀는 **오토타일 + 유기적 ragged 엣지**로 모든 경계를 녹인다. 안식은 전 피처가 하드컷:
- **길↔지면:** `_build_path_grass_fringe` 물결이 tan-tan에선 안 읽힘 → 길 양옆에 **명암/톤 대비 + raggedness** 강화, 잔디 base(A안)면 길↔잔디 프린지로 자연.
- **밭(SOIL) 외곽·타일간:** 오토타일(Wang) 미적용 → **밭 = 연속 오토타일 필드**로(내부 furrow 격자만 남기고 외곽·타일경계 매끈). owner "따로따로" 직결.
- **연못·절벽·건물 패드:** 아래 P3·P4·P7.

### P2 · 밭(SOIL) 오토타일 — "따로따로" 해소 (효과 큼·비교적 국소)
현재 SOIL 타일이 낱개 스프라이트로 격자 갭. **연속 tilled 필드**(Wang/오토타일)로 교체 — 내부는 furrow 라인, 외곽은 흙↔지면 유기 전환. 스타듀 hoe-dirt와 동형.

### P3 · 물 — 정사각 연못 → 유기형 + 오토타일 물가
현재 하드 직각 박스. **유기적 윤곽 + 물↔지면 Wang 물가**(반영·리플 엣지). [ADR-0044] 남향 물 토폴로지(강/개울)와 정합 검토. 스타듀 연못=곡선 shore.

### P4 · 절벽/고지 — 직사각 "구덩이" → 유기 절벽
좌측 2개 직사각 구덩이가 구멍처럼. **유기적 절벽 윤곽 + 초록 top lip + 그림자 접지**([ADR-0056]는 남단만 처리). 동단 "잔디 능선" 평평 seam은 dissolve로 완화됐으나, 절벽 자체 형태가 기하학적. 스타듀 절벽=불규칙 곡선.

### P5 · 경계 덤불 벽 — 일렬 → 유기 배치
완벽한 세로 일렬 덤불 = 인공. 스타듀 경계=불규칙 나무선/절벽. **덤불·나무를 불규칙 클럼프로 산개**(통행 차단은 유지하되 라인 깨기), 또는 절벽/울타리로 대체.

### P6 · 레이아웃/조닝 — 균일 빈 평원 → 목적 있는 zone
현재 광활한 빈 tan. 스타듀 팜=암시적 zone(농사·목축·과수·물). **의도적 구역화**(밭 블록·목초지·과수원·연못 주변)로 "빈 곳" 감소. C안(base 이원화)과 시너지.

### P7 · 건물 접지 — 하드 직사각 패드 → 유기 접지
`_g16_near_building` 패드가 직사각. **발치 유기 흙 번짐 + 그림자**로 "심어진" 느낌([ADR-0054] 정신 확장).

### P8 · 밀도·변주 잔여 — 스캐터 자체는 개선됨(이번 작업)
풀tuft 분포·clump은 이번 dissolve+CA로 나아짐. base(P1 crux) 해결 시 체감 급상승. 변종 확대(로스터 §)는 병렬 지속.

---

## 3. 권고 순서

1. **crux(§1) 먼저 결정** — A/B/C. 이게 안 정해지면 P1·P3·P4 형태가 안 잡힌다.
2. crux 후 **P2(밭 오토타일)** = 국소·효과 큰 quick win, owner "따로따로" 직결.
3. **P1(경계 유기화)** = 광범위·crux 의존(잔디 base면 프린지가 살아남).
4. P3·P4(물·절벽 유기형) → P5(덤불) → P6(조닝) → P7(접지).

각 항목은 [ADR-0057](Wang)·[ADR-0044](물)·[ADR-0056](절벽)·[ADR-0043](지형)·[ADR-0025](에셋 스펙) 계보 위에서 슬라이스로 분해. **crux 결정이 곧 새 ADR(0059?)**.

---

## 4. 진행 로그 (2026-07-17 라이브 세션 — 워크트리 worktree-scatter-variation-adr0058)

**✅ 완료(commit f1f151b, 회귀 3/3):**
- **P1 경계 유기화(잔디↔흙)** — Wang 손그림 불일치("다른 잔디") 근절: **전환 타일을 라이브 base(`_bf_grass`/`_bf_earth`) 픽셀에서 합성**(`_bake_grass_dirt_wang`, owner "B·불일치-불가" 결정). 같은 톤 보장·소프트 래그드 엣지·1px 어두운선.
- **crux 잔디** — Forest Farm 레퍼런스대로 흙-지배 base + **유기 잔디뭉치 부활**(`_G16_GRASS_PATCHES` 재활성·CA 클럼프·문턱 0.68). 초원 하드블록 dissolve.
- **입체감** — 잔디 남향 드롭섀도("잔디가 흙 위에 자라난", owner "적당해" 확정).
- **길 정리** — 길(2) 경계 Wang 스킵 = 길 깨끗(잔디 안 얹힘).

**🔴 다음 세션 최우선 — 밭(SOIL) waffle "따로 떨어진 짙은 타일"(owner 스샷 2026-07-17 13:41):**
- 증상: 스타터 밭(STARTER_PATCH 5×5)이 **어두운 초록 낱개 사각형 + 셀 갭 격자**(waffle)로 렌더 — 이어붙지 않음.
- 진단: `soil_field.png`(=`_bf_soil` base)는 **순수 갈색**(70,34,9)이라 그 초록 waffle의 소스가 아님. SOIL 렌더가 [터레인 타일셋 TR_SOIL autotile(`ground` TileMap) + `field_layer` 오버레이(`_build_field_tileset`·`_extract_soil_base`) + `_bf_soil` 빅필드] 3중으로 얽혀 있고, 셀-정렬 격자/씸이 grass가 겪던 노이즈-카펫/씸 부류 문제를 SOIL이 아직 겪는 것. **색 불일치가 근원 규명을 요구**(비-헤드리스 실캡처 or 각 레이어 토글 진단).
- 해법 방향(자체 슬라이스): SOIL을 grass처럼 **seamless**로 — ⓐ base 재생성(저색 crisp, 격자/테두리 제거) 또는 ⓑ 오버레이/타일맵을 잔디↔흙처럼 **base 합성 전환**으로 연결. 세션 끝 5분 수정 불가(농사 시각화 리스크)라 defer.

**미착수(우선순위 §3):** P2 밭 오토타일(위) → P3 물 유기형 → P4 절벽 유기형 → P5 일렬 덤불→나무/절벽 경계 → P6 조닝 → P7 건물 접지. 잔여 아티팩트: 잔디 속 밝은 tan 구멍(고립 흙 셀·근원 미규명).

---

# 5. Slice 1R 재검증 (2026-07-22 · Fable 5 캠페인 격차 감사 — 맵 축)

> **방법:** 코드 실좌표 검증(main.gd) + `tools/home_full_dump.png`(f48a24a 직후 — 이후 커밋 a495c30은 타일 톤만 변경, 매크로 배치 불변) + 스타듀 위키 미러 Farm 문서 대조. 산출 워커=audit-map(Opus).
> **결론:** P2·P7·P8 해소 / **P3·P4·P5·P6 미해소**. 신규 최대 격차 = **C-1 "북단 밀집/남단 공동" 비대칭 레이아웃**.

## 5.1 P1~P8 현재 상태 검증

| 항목 | 2026-07-17 상태 | 현재 코드 검증 결과 | 분류 | 근거 |
|---|---|---|---|---|
| **P1** 하드 경계 전면 | 🔴 전 피처 하드컷 | **부분 해소.** 잔디↔흙=`_bake_grass_dirt_wang`+월드위상 셀별 합성(6b7880c·888f50f). 밭·길=직선사각+소프트닝(f48a24a). **물·절벽·덤불 경계는 미해소** | 재작업(잔여) | main.gd:3951,4010-4014 |
| **P2** 밭 오토타일 "따로따로" | 🔴 낱개 waffle | **해소(방식 전환).** 밭=Wang 스킵→base blit 직선 사각+`_soften_field_edges`. 덤프에서 연속 furrow | **유지** | main.gd:4013; f48a24a |
| **P3** 물 유기형 | 🔴 정사각 박스 | **미해소.** SPIRIT_POND_RECT(26,34,8,7)=사각 fill. `_autotile_pond_siblings`가 **북단 1변만** bank. 나머지 3변 하드 사각 | 재작업 | main.gd:2749,3089-3093,904 |
| **P4** 절벽 유기형 | 🟠 직사각 구덩이 | **부분 해소.** 남향 오토타일러+곡선코너+LIP평지화+접지그림자(ADR-0056). 그러나 **고지 마스크가 사각Rect**(x≤20,y≤26)라 좌측 절벽 2개 여전히 직사각 | 재작업 | main.gd:3030-3038,3100-3119,3024 |
| **P5** 덤불 벽 일렬 | 🔴 완벽 세로 일렬 | **미해소.** PROP_LAYOUT_HOME x고정·y1~26 13개 세로 벽(x21 seam 능선) | 재작업 | main.gd:621,684-687,3125 |
| **P6** 조닝 균일 빈 평원 | 🔴 광활한 빈 tan | **미해소·재정의.** "균일"이 아니라 **북단 밀집/남단 공동** 비대칭(→C-1) | 재작업(신규) | 건물 rect@827-982,필드@899 |
| **P7** 건물 접지 하드 패드 | 🟠 직사각 tan 패드 | **해소(정합).** `_g16_near_building` 맨흙 패드+facade 그림자(ADR-0054). 직사각이나 owner 수용 | **유지** | main.gd:3977-3980,4293-4306 |
| **P8** 밀도·변주 | 🟡 개선됨 | **해소(진행).** 스캐터 밀도 양호. 변종 확대=병렬 아트(ADR-0058) | **유지** | ADR-0058 |

## 5.2 스타듀 농장 문법 대조 (위키 근거)

- **진입 동선:** 스타듀는 시작 시 **길이 없다**(플레이어가 Flooring으로 저작). 안식은 두꺼운 tan 십자 길 고정 저작(중앙 세로 스파인+가로 복도)이 맵을 사분면으로 가르고 남향 스파인은 목적지 없이 뻗음. → **재작업**(폭 축소·유기화, 건물 연결 스퍼만 남기고 남향 스파인은 남단 조닝과 연결).
- **농지 블록:** 스타듀 시작 tilled 0·자유 분산 개간. 안식 STARTER_PATCH 5×5(40,12)+대형 밭 1블록이 건물과 함께 북단에 갇힘. → **재작업(경미)**, P6 흡수.
- **건물 군집:** 스타듀는 팜하우스만 고정·나머지 전역 자유 배치. 안식 6동 전부 y2~20 북단 밴드 조밀, 남단 0. → **재작업**.
- **구역 리듬:** 스타듀는 절벽·연못·나무·건물이 전역 리듬. 안식은 북단 압축·남단 리듬 없음. → **재작업**(P6).
- **오픈스페이스 비율:** owner 레퍼런스 Forest Farm="숲에 안긴" 밀도(서측 나무 밀집대·추가 절벽·연못). 안식은 남단 55% 미분화 개방, **나무 밀도 크게 부족**. → **재작업**.

## 5.3 신규 발견 격차

- **C-1 (최대) 북단 밀집/남단 공동.** 건물 6동(y2~20)·STARTER_PATCH(y12~16)·대형 밭이 상위 30%에 몰리고 연못(y34~40)·서측 절벽 외 **y20~65(맵 55%)에 설계 피처 0**. 기존 P6은 증상("빈 평원")만 봤고 근본은 "압축+공동" 비대칭.
- **C-2 Forest Farm 나무 밀도 미달.** 크럭스 확정이 요구한 "사이드 빽빽한 나무+내부 밀집" 대비 라이브는 perimeter 산발 소수. 남단 공동을 채울 **가장 국소적·저위험 레버**(PROP_LAYOUT_HOME 스캐터 추가·좌표 테스트 무관).
- **C-3 문서 모순: `_G16_GRASS_PATCHES`.** ADR-0058 불변식은 `=false 유지` 명시, 현행 코드는 `:= true`(main.gd:4175·크럭스 결정 반영). 라이브는 true가 이김. **맵 작업 착수 전 ADR-0058 개정(또는 크럭스 결정의 새 ADR 승격)으로 단일 진실원 복구 필요.**
- **C-4 남향 스파인 목적지 없음.** 중앙 세로 길이 y40+에서 목적지 0. ADR-0044 "남향 물 축"은 월드 그래프이지 HOME 내부가 아님 → HOME 남단 목적존은 스스로 설계(C-1 연동).
- **C-5 연못이 활동존과 격리.** 연못(낚시·물뿌리개 앵커, Slice 3 예약)이 밭·건물에서 멂. `_autotile_pond_siblings` 주석의 "좌표 바이트 동일=save/앵커 불변" 요구 → P3 유기화는 형태 확장만 하고 rect 앵커 보존.

## 5.4 재작업 우선순위 (효과 대비 국소성)

1. **[C-1/P6] 남단 조닝** — 최대 효과. y20~65에 목적존(과수원·목초·추가 개간지·forage 숲) 손설계. 국소성 중(레이아웃 상수 추가→테스트 갱신). **선행 필수**.
2. **[C-2] Forest Farm 나무 밀집대** — 효과 큼·**최저위험**(스캐터만·좌표 테스트 무관). Quick win, 존 경계 자연 프레이밍.
3. **[P5] 덤불 벽 유기화** — 국소적(덤불 좌표만·능선 충돌바 유지). 세로 일렬→불규칙 클럼프.
4. **[P3/C-5] 연못 유기형** — shore 4변 오토타일+blobby 윤곽. rect 앵커·북단 bank 보존.
5. **[P4] 절벽 형태 유기화** — is_hi 마스크 사각Rect→불규칙 윤곽(오토타일러는 마스크 콜백만 교체 재사용).
6. **[P1 잔여/진입동선] 십자 길 폭축소·유기화** — 1에 종속.

불변 준수: ADR-0044 pseudo-Z·남향 물 축, ADR-0053 흙-지배 base. 재론 금지 항목(지상 WFC·듀얼그리드·물가 Wang합성·밭 래그드·16px) 미저촉 — 스캐터/손설계 매크로/형태 마스크 교체만 사용.

## 5.5 좌표 의존 헤드리스 테스트 (재배치 시 깨질 후보)

| 테스트 | 의존 | 위험 |
|---|---|---|
| `home_expansion_test.gd` | HOUSE_EXT_RECT·STOREHOUSE_RECT·STARTER_PATCH_RECT·워프칸(78,32)·건물 도달성 BFS | **최고**(36 hits) |
| `livestock_test.gd` | 넋우릿간/넋둥우리/silo 좌표·동물 pathing | **최고**(38) |
| `reclaim_test.gd` | 개간 후보 좌표·ENCROACH_SCAN_RECT·성역 배제 | 높음(16) |
| `well_test.gd` | WELL_RECT(40,18) 박스·접근 | 높음(15) |
| `cliff_test.gd` | 자체 scratch rect 사용→HOME 재배치 안전, 오토타일러 로직 변경 시만 | 중 |
| `orchard_test`(8)·`crow_test`(5)·`building_grounding_test`(5)·`warp_test`(4) | 과수·까마귀 반경·접지 패드·워프 | 중~저 |
| `scatter_variation_test`·`flower_patch`·`forage` | 스캐터/채집(상대·결정성) | 저 |

**주의:** 스캐터·형태 마스크는 결정적 좌표 해시 시드만 사용(ADR-0058 불변식). 남단 조닝으로 건물/밭 rect 이동 시 상단 4개 테스트 하드코딩 좌표 + `_autotile_pond_siblings`·`_g16_near_building` rect 상수 동반 갱신.

> 농사·UI 축 감사 = [slice1r-stardew-gap-audit.md](./slice1r-stardew-gap-audit.md). 타일맵 알고리즘 서베이 = [tilemap-generation-algorithms-survey.md](./tilemap-generation-algorithms-survey.md).
