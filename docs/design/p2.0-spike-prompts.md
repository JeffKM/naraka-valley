# P2.0 스파이크 — PixelLab 프롬프트 초안

> 재시작·MCP 승인 후 그대로 호출/복붙할 수 있는 초안. 프롬프트 문자열은 **영어**(PixelLab 모델이 영어 학습이라 결과가 나음), 설명은 한국어.
> 목표 = 얇은 단면(미호 + 혼령초 3단계 + 풀/길/흙 타일)으로 PASS 4기준 판정 + 스타일 가이드 잠금.
> 산출 PNG는 `game/spike/assets/`에 README의 이름대로 저장 → `asset_preview.tscn`(F6)로 적용·판정.

## 0. 스타일 잠금(모든 프롬프트에 공통으로 녹일 것)

- **규격:** 캐릭터 16×32(약 2.5등신 치비), 타일 16×16, 탑다운 3/4뷰, 내부해상도 320×180(정수배). (ADR-0003)
- **외곽선 = 역할별 선택적:** 전경(캐릭터·작물) = *어두운 같은 계열색* 외곽선(순검정 아님) / 배경(타일) = **외곽선 없음**(텍스처+음영, 격자 떡짐 방지).
- **팔레트 의도:** 저승·명부 톤 — 차분한 채도 + 푸른 "영혼빛" 액센트. 색 수 절제(limited palette). *정확한 마스터 팔레트는 스파이크가 발견해 잠근다.*
- **음영:** 2~3단 램프(과한 그라데이션 금지).

> 아래 프롬프트엔 위 의도를 영어로 박아뒀다. 1차 결과를 보고 팔레트/외곽선 강도를 조여 재호출 → 통하는 레시피를 이 문서에 확정 기록한다.

## 1. 미호 — 4방향 워크 (MCP: `create_character` → `animate_character`)

미호 정체성(CONTEXT): 구미호, 노란 한복, 여우귀, 아홉 꼬리·여우불(파란 불꽃), 밝고 해맑음.

```
create_character(
  description="cute cheerful nine-tailed fox girl, yellow hanbok (Korean dress),
    fox ears, hint of fluffy fox tails, small blue foxfire spark accent,
    top-down 3/4 JRPG view, ~2.5 head chibi proportions, 16x32 pixel sprite,
    selective dark tonal outline (not pure black) on silhouette, limited muted
    underworld palette with soft blue spirit accent, 2-3 tone shading, clean readable at small size",
  n_directions=4
)
# → 반환된 character_id로:
animate_character(character_id="<위 id>", animation="walk")
```

- 저장: 4방향 워크 시트를 `miho_walk.png`로. 시트 행/열 배치가 하네스 가정(행=방향 down/up/right/left, 열=프레임)과 다르면 `asset_preview.gd`의 `CHAR_DIRS`·`CHAR_FRAMES_PER_DIR` 조정.
- 판정 포인트: ③ 정체성(여우귀·한복이 16×32에서 읽히나) + ① 4프레임 일관성.

## 2. 혼령초 3단계 (REST API 일반 생성 — PixFlux/BitForge text-to-pixel)

MCP 4툴엔 임의 오브젝트 생성이 없으니 **API 일반 생성 엔드포인트**로(작물·소품은 전부 이 경로). 혼령초(CONTEXT): "푸르게 빛나는 영혼 풀", 빠름·저수익 입문 작물.

```
# 단계별 1장씩, 16x16
seed   : "tiny planted seed mound, top-down, 16x16 pixel, dark tonal outline,
          muted soil with faint blue spirit glow, 2-tone shading"
sprout : "small glowing blue spirit grass sprout, two thin luminous blades,
          top-down, 16x16 pixel, dark tonal outline, soft blue glow, 2-3 tone shading"
mature : "harvest-ready glowing blue spirit grass, full luminous tuft, clearly distinct
          from sprout, top-down, 16x16 pixel, dark tonal outline, bright blue spirit glow"
```

- 저장: `honryeong_seed.png` / `honryeong_sprout.png` / `honryeong_mature.png`.
- 판정 포인트: 세 단계가 *한눈에 구분*되나(특히 수확가능). 전경 외곽선이 타일 위에서 사나.

## 3. 풀↔길↔흙 타일 (MCP: `create_tileset` — Wang 전환)

P2.3에서 정한 "중간 오토타일(경계·모서리 전환)"의 스파이크판. 배경이므로 **외곽선 없음**.

```
create_tileset(lower="dirt path", upper="grass")     # 풀↔흙길 전환
create_tileset(lower="tilled farm soil", upper="dirt path")  # 길↔밭흙 전환
# 공통 의도: top-down 16x16, no black per-tile outline, textured fill with 2-3 tone shading,
#            muted underworld palette, seamless tiling
```

- 저장: 전환 세트에서 베이스 타일 3종을 잘라 `tile_ground.png`(풀) / `tile_path.png`(흙길) / `tile_soil.png`(밭흙). (전환 모서리 타일은 P2.3 본작업에서 Godot terrain set으로.)
- 판정 포인트: ② 타일 위에 미호·혼령초가 묻히지 않고 붙나 + 타일 이음매·격자 떡짐 없나.

## 4. 판정 결과 — ✅ PASS (2026-06-21, 잠금)

미호 + 혼령초 3단계 + 풀/흙길/밭흙 타일 7에셋을 `asset_preview.tscn`에서 PASS 4기준 통과(가독성·톤결합·미호 정체성·비용 체감). 아래 레시피를 **P2.1~P2.3 벌크가 그대로 복제**한다.

### 4.1 통한 프롬프트·파라미터 최종본 (실제 호출값)

**미호 — `create_character` → `animate_character`** (PixelLab MCP)
```
create_character(
  description="cute cheerful nine-tailed fox girl, bright yellow hanbok (Korean traditional dress),
    fox ears, fluffy fox tails, small blue foxfire spark accent, muted underworld palette with
    soft blue spirit accent, selective dark tonal outline not pure black, 2-3 tone shading,
    clean and readable at small size",
  n_directions=4, size=32, view="low top-down",
  outline="selective outline", shading="basic shading", detail="high detail",
  proportions={"type":"preset","name":"chibi"}
)
animate_character(character_id="<id>", template_animation_id="walk")   # 4방향, template 모드
```

**혼령초 3단계 — `create_map_object`** (단계별 1회씩, 공통 파라미터)
```
공통: width=32, height=32, view="low top-down", outline="selective outline", shading="basic shading"
seed   : "tiny planted seed mound, muted dark soil with faint blue spirit glow" (detail="low detail")
sprout : "small glowing blue spirit grass sprout, two thin luminous blades, soft blue glow"
mature : "harvest-ready glowing blue spirit grass, full luminous tuft clearly bigger than a sprout,
          bright blue spirit glow"
```

**지형 타일 — `create_topdown_tileset`** (Wang, lineless)
```
공통: view="low top-down", outline="lineless", shading="basic shading", tile_size=16x16
풀↔흙길 : lower_description="dirt path", upper_description="grass"
흙길↔밭흙: lower_description="dirt path", upper_description="tilled farm soil with furrows",
           lower_base_tile_id=<풀↔흙길의 dirt base id>   # ★체이닝으로 이음매 일치
```
→ 타일셋은 16타일 시트(64×64)로 나온다. **순수 타일**(corners 4개가 전부 같은 terrain)을 metadata의
   `bounding_box`로 16×16 크롭해 `tile_ground/path/soil.png` 추출(글루, ADR-0001 허용).

### 4.2 마스터 팔레트 (실측 hex, 8단위 양자화)

| 역할 | hex | 비고 |
|---|---|---|
| **전경 외곽선** | `#401818` 계 (적갈) | ★순검정 아님 — ADR-0003 "어두운 계열색" 의도 일치 |
| 미호 한복(노랑) | `#f8d060` | 액센트 흰 `#f8f8f8`, 피부/디테일 주황 `#f89048` |
| 영혼빛 파랑(저채도→고채도) | `#60d8f0`(sprout) → `#2068e8`(mature) | 혼령초·여우불 공통 영혼빛 액센트 |
| 흙길·밭흙(차분 갈색) | `#684840` / `#503030` | 톤 OK |
| 풀(녹색) | `#10c800` | ⚠️ **쨍한 형광** — "차분한 채도" 의도와 어긋남. P2.1 채도 하향 1순위 |

### 4.3 비용·시간 (실측)

- **총 13 generations / 핵심 7에셋** (트라이얼 무료 40 gen 중). 내역:
  - `create_character`(standard 4-dir) = **1 gen**
  - `animate_character`(walk template) = **방향당 1 gen → 4방향 4 gen**
  - `create_map_object` = **에셋당 1 gen** (혼령초 3 = 3 gen) ← 작물·소품의 정답 경로
  - `create_topdown_tileset` = **셋당 2~3 gen** (타일 2종 산출 → 타일당 ~1.25 gen)
- wall-clock: 캐릭터 ~3분, walk 4방향(병렬) ~4분, map_object/타일셋 ~90~100초. 폴링 대기 포함 세션 ~15분.
- **외삽 경고:** 트라이얼 40 gen은 스파이크용. P2.1~P2.3 벌크(NPC 4종 × 다방향/다애니 + 작물 다수 + 타일 다수)는 40 gen을 초과 → **유료 구독/크레딧 전환 필요**. gen당 단가는 위 내역으로 산정.

### 4.4 발견사항 (하네스·파이프라인에 반영 완료)

- **캐릭터 규격:** `size=32` 요청 → PixelLab이 캔버스를 ~1.4배 키워 **48×48 프레임** 출력. walk 템플릿은 **방향당 6프레임**. → `asset_preview.gd`의 `CHAR_FRAME=48×48`, `CHAR_FRAMES_PER_DIR=6`로 잠금. (16×32 타겟과 다름 — 벌크에서 Aseprite 다운스케일 또는 규격 재협의 필요.)
- **출력 포맷:** 캐릭터는 시트가 아니라 **방향별 개별 PNG**(south/north/east/west × 프레임). → 4행(down/up/right/left)×N열 시트로 합성하는 글루 필수.
- **작물 규격:** `create_map_object` 최소 캔버스 32px → 16 타겟과 불일치. 현재 32px 원본 사용(하네스는 바닥정렬로 수용). 벌크는 트림+다운스케일 단계 필요.
- **금지:** `create_1_direction_object`는 20~40 gen(트라이얼 몰살) → 절대 사용 금지. 작물·소품은 `create_map_object`.
- **타일셋 체이닝:** 인접 지형은 이전 타일셋의 base_tile_id를 `lower_base_tile_id`로 넘겨 이음매 일치.

### (참고) FAIL이었다면
손절 사다리(ROADMAP P2.0) — ① 스타일 완화 재스파이크 → ② 범위 축소 → ③ 폴백(외주/유료팩/제미나이 img2pixel는 PixelLab 호스트 변환이라 ADR 무방).

---

## 5. P2.1 캐스트 벌크 — 추가 레시피·발견 (2026-06-21)

플레이어 + 미호·옥자·바나·멜 5종을 `game/assets/characters/`에 도색. 미리보기는 `game/cast_preview.tscn`.

### 5.1 캐스트 비주얼 소스
- **원작 IP 일러스트가 진실의 원천.** 옥자·바나·멜·미호는 원작 컨셉카페 캐릭터 그림(제미나이 픽셀 일러스트)을 받아 그 외형을 영어 description으로 옮겨 `create_character`에 박았다(이미지 직접 입력 아님 — 텍스트 변환).
  - 미호: 검은 장발·흰 여우귀·**흰 저고리+노란 치마**·흰 여우꼬리·파란 여우불 (P2.0의 "노란 한복 전체"는 추측이라 폐기).
  - 옥자: 검은 마녀모자(빨간 깃털)·와인 곱슬 장발·안경·버건디 빅토리안 드레스.
  - 바나: 붉은 눈·송곳니·금발+검정 투톤·보라 고딕로리타.
  - 멜: 청록 만주 치파오·파란 술 만주모자·검은 단발.
- **플레이어 = 의도적 무채.** 원작 비주얼 없음 → 죄목("조용한 죄=빈칸") 기반 중립 디자인(남, 다크브라운 단발, 저채도 평상복). 후속 팔레트 스왑 베이스.

### 5.2 ★ hole-fill 후처리 (필수 — 안 하면 "속 비고 타일 비침")
PixelLab 캐릭터/워크 프레임은 **외곽선에 갇힌 투명 구멍**을 자주 남긴다(특히 정면 워크의 흰 꼬리 속, 미호 정면 9프레임에서 275px). 단순 alpha(투명/불투명) 검사로는 안 잡히고 **flood-fill로 "셀 경계와 연결된 외부 투명" vs "갇힌 내부 hole"을 구분**해야 보인다.
- 파이프라인: 시트 48px 셀마다 ① 경계에서 flood-fill로 외부 투명 마킹 → ② 남은 투명 = hole → ③ hole을 인접 불투명색으로 dilation 채움(흰 꼬리는 흰색으로 메워짐).
- 전 캐릭터 일괄 적용(미호 275·플레이어 3·옥자 2). **검증 = hole 0**.

### 5.3 워크 부위 일관성
- 가는 부위(꼬리·귀)는 **template walk가 프레임마다 크게 펄럭/출렁**여 깜빡인다 → **`mode="v3"` + `action_description`에 "single tail steady, no sway, ears fixed"**가 더 안정. v3는 방향당 9프레임(1 ref+8).
- **꼬리는 1개로 디자인**(description에 "exactly ONE single fox tail")하면 좌우 펄럭 원인 자체가 줄어든다(아홉 꼬리는 48px에서 무리).
- 상주 NPC(옥자·바나·멜)=rotation 4방향 1프레임 **idle**(애니 없음) / 움직이는 플레이어·미호=**v3 walk**.

### 5.4 Godot import 갱신 함정
PNG를 **같은 이름으로 교체**하면 Godot이 옛 import 캐시를 써 로드 실패/구버전 표시. → `assets/.../X.png.import` 삭제 + `.godot/imported/X.png-*` 삭제 후 `godot --headless --import` 재실행.

### 5.5 실행 글루
긴 절대경로를 셸에 복붙하면 줄바꿈으로 깨진다 → `game/run_cast.sh`(내부에서 `--path $PWD --resolution`)로 감싸 `bash <절대경로>` 한 토큰으로 실행. 캐릭터 시트 규약은 P2.0과 동일(48×48, 행=down/up/right/left), 단 워크 열 수는 시트 너비÷48로 자동 인식.

---

## 6. P2.2 작물 벌크 — 추가 레시피·발견 (2026-06-21)

피안화·영혼 호박 각 3단계 = 6 gen을 `create_map_object`로 도색, `game/assets/crops/`에 저장. 미리보기는 `game/crop_preview.tscn`(`bash game/run_crops.sh`). 혼령초(P2.0)까지 3작물 × 3단계가 한 화면에서 비교된다.

### 6.1 통한 프롬프트 (실제 호출값, §4.1 공통 파라미터 그대로 복제)
공통: `width=32, height=32, view="low top-down", outline="selective outline", shading="basic shading"`. **씨앗=low / 새싹=medium / 수확=high detail.** 작물 정체성(CONTEXT '저승 작물')을 description에 박고, **영혼빛 액센트 색을 작물마다 다르게** 줘 단계 구분 + 작물 구분을 동시에 만든다(혼령초=파랑·피안화=빨강·호박=주황).

```
피안화(彼岸花, 저승 상징 붉은 꽃):
 seed  : "tiny planted seed mound in dark furrowed soil, faint crimson red spirit glow seeping from the seed"
 sprout: "small young sprout with a thin dark-green stalk and a closed crimson flower bud beginning to form, faint red spirit glow"
 mature: "harvest-ready blooming red spider lily higanbana flower, distinctive long curled radial spidery petals
          and upward arching stamens, clearly a full vivid red flower much bigger than a sprout, crimson red spirit glow"
영혼 호박(Soul Pumpkin, 얼굴이 살짝 비치는 호박):
 seed  : "tiny planted seed mound in dark furrowed soil, faint warm orange spirit glow seeping from the seed"
 sprout: "small pumpkin vine sprout with a few broad green leaves and a tiny round green gourd starting to swell, faint orange spirit glow"
 mature: "harvest-ready large round orange pumpkin sitting on the ground, a faint ghostly spectral face softly glowing
          through the rind like a jack-o-lantern, clearly much bigger than a sprout, soft blue-white soul glow around the face"
```
공통 꼬리: `muted underworld palette, selective dark tonal outline not pure black, 2-3 tone shading, top-down`. **"clearly much bigger than a sprout"가 수확 단계 크기 점프를 만든다**(§4.1 혼령초 mature와 동일 트릭).

### 6.2 ★ 결과 PNG 다운로드 글루 (P2.0엔 미기록 — 이번에 확정)
`create_map_object`는 ID만 즉시 반환 → `get_map_object(id)`가 `status: completed`일 때 **`download:` URL**(`https://api.pixellab.ai/mcp/map-objects/<id>/download`)을 준다. MCP 이미지 블록은 디스크 저장 불가이므로 **그 URL을 curl로 받는다**(ADR-0001 허용 글루 = 받기·임포트):
```
curl -s -o assets/crops/<name>.png -H "Authorization: Bearer $PIXELLAB_API_KEY" \
  "https://api.pixellab.ai/mcp/map-objects/<id>/download"
```
키는 `.claude/settings.local.json`의 `env.PIXELLAB_API_KEY`에서 셸 변수로만 읽고 **절대 출력/커밋하지 않는다**. 결과는 32×32 RGBA(요청 그대로), 하네스가 바닥정렬로 수용 → 다운스케일 불요. **map object엔 §5.2 hole 없음**(flood-fill 검사 0 — 외곽선에 갇힌 투명 구멍은 캐릭터 워크 프레임 고유 문제).

### 6.3 파일명 = CropCatalog 영문 id 정렬
미래 T2.3 성장 로직이 `"res://assets/crops/%s_%s.png" % [crop_id, stage]`로 로드하도록 **파일 stem = `crops.gd`의 id**(`honryeongcho`·`pianhwa`·`yeonghon_hobak`) + `_seed/_sprout/_mature`. 혼령초 P2.0 산출(`honryeong_*`, 스파이크 폴더)도 `assets/crops/`로 옮기며 `honryeongcho_*`로 정규화(스파이크 `asset_preview.gd`가 참조하는 원본은 그대로 둠).
- **⚠️ seam:** 아트는 작물당 3프레임인데 `crops.gd` `stages`는 혼2/피3/호4. T2.3이 N 논리단계를 3 아트프레임에 매핑해야 한다(예: 마지막 프레임=수확가능 고정, 중간은 보간/반복). P2.2 범위 아님 — 기록만.

### 6.4 운영 발견
- **rate limit:** 6건을 한 번에 던지면 4건만 받고 2건 `rate limit exceeded`(~15–30s 후 재시도하면 통과). 벌크는 4건씩 끊어 던지는 게 안전.
- **서버측 실패:** 1건이 `[500] Out of CUDA memory`로 실패(내 파라미터 무관) → 동일 프롬프트 재생성으로 통과. 폴링 시 `failed` 상태를 반드시 확인하고 재던질 것.
- **비용:** 6 gen 사용(재시도 1 포함 실측 7 gen). 구독 Tier 1(2000 gen/주기)로 P2.0 트라이얼 경고(§4.3) 해소 — 잔량 충분.

---

## 7. P2.3 단계① 지형 오토타일 — 레시피·★Godot terrain 함정 (2026-06-21)

풀↔길↔밭 3지형을 **Godot terrain 전환**(corner 오토타일)으로. PixelLab Wang 타일셋 3세트를 공식 컨버터로 합쳐 `game/assets/tiles/combined_terrain.tres`(3 terrain) 생성, `main.gd`의 `_build_tileset`/`_paint_grid`가 이를 로드해 맵을 칠한다. 미리보기는 `tools/map_dump.gd`(렌더 없이 타일 배치를 PNG로 합성 — 헤드리스 GPU 불요).

### 7.1 통한 레시피 (실제 호출값)
- **3 전환 세트 = `create_topdown_tileset`**(`outline="lineless"`, `shading="basic shading"`, 16×16, `view="low top-down"`). base 체이닝으로 이음매 일치:
  - 풀↔흙길: `lower="dirt path"`, `upper="muted desaturated mossy grass, low saturation somber underworld graveyard green, ..."`, `lower_base_tile_id=<기존 dirt path base>`
  - 밭흙↔풀: `lower="tilled farm soil with furrows"`, `upper=<같은 풀 문구>`, `lower_base=<밭흙 base>`, `upper_base=<풀 base>` ★풀↔밭 직접 경계용(밭이 풀 한가운데 박혀 직접 닿음)
  - 흙길↔밭흙: P2.0 산출 재사용(`path` base 공유로 이음매 일치)
- **다운로드:** `get_topdown_tileset`이 준 `download_png`/`download_metadata`는 **302 redirect(backblaze)** → `curl -L` 필수(없으면 0바이트). 파일명은 컨버터 규칙 `*_metadata.json`/`*_image.png`로.
- **컨버터:** PixelLab 공식 `tools/pixellab_tileset_converter.gd`(MCP `pixellab://docs/godot/wang-tilesets`를 벤더링, output만 `res://`로). 인자 순서가 terrain id를 고정(0=길·1=풀·2=밭). 한 번에 3쌍 → corner peering bit 자동 설정된 단일 `.tres`.

### 7.2 ★ Godot terrain 함정 (이게 막혔던 핵심 — 다음 지형 작업에 그대로 적용)
1. **`set_cells_terrain_connect(cells, set, terrain, ignore_empty_terrains=false)`** — 4번째 인자 **반드시 false**. 기본 true면 빈 캔버스 첫 칠에서 모든 이웃이 empty라 제약이 사라져 **한 칸도 안 칠해진다**(by_source에 terrain 0칸). false라야 base가 매칭됨.
2. **모드 = `TERRAIN_MODE_MATCH_CORNERS`(1).** 컨버터 `.tres`는 `mode=0`(CORNERS_AND_SIDES)으로 나오지만 Wang은 코너만 설정 → side 미설정으로 매칭 깨짐. 로드 후 `set_terrain_set_mode(0, MATCH_CORNERS)` 강제.
3. **칠 순서:** 모든 지형 칸을 *풀 하나로 먼저* 단일 칠(연속 영역이라 검은 구멍 0) → 밭을 얹어 전환 → **단색(HOUSE/CAFE/WALL)은 맨 나중에 set_cell**. 단색을 먼저 깔면 그 terrain 없는 타일이 인접 풀의 corner 매칭을 깨 빈칸(검은 구멍)을 남긴다.
4. **1칸 폭 동선(길)은 base 직접 set_cell.** corner 전환은 양옆이 풀이라 길이 풀로 묻힌다 → `_terrain_base_atlas(TR_PATH)`로 또렷한 흙길 base를 직접 깐다(전환 없이). 넓은 밭은 terrain 전환 유지(경계 부드러움).

### 7.3 풀 채도 후처리 (P2.0 '하향 1순위' 이행)
`create_topdown_tileset` description만으론 풀이 여전히 순녹(#048e0c). `tools/desaturate_grass.py`(PIL, ADR-0001 허용 색보정 = Aseprite 보정)로 **녹색 hue(80–175°) 픽셀만** 채도×0.55·명도×0.82 → `#306033`(어두운 이끼). 흙(갈색)은 hue로 보존. 원본은 `*_raw.png`로 1회 백업(idempotent). 보정 후 컨버터 재실행 → import.

### 7.4 흙 상태(DRY/WET) 도트
`_build_field_tileset`이 단색 고랑 대신 **밭흙 terrain base를 추출**(`_extract_soil_base`)해 DRY=어둡게(×0.80)·WET=짙고 푸르게(×0.55 +blue)로 틴트. 무외곽선. 가운데 새싹 점은 성장 표시 그레이박스 유지(P2.2 작물 스프라이트 연결은 T2.3 seam).

### 7.5 비용·범위
- gen: 신규 2세트(풀↔길·밭↔풀) ≈ 5 gen. 흙 상태·풀 채도는 gen 0(파생·후처리).
- 단계 ① 범위 = 지형·흙만. **실내 바닥·벽·가구·장식(HOUSE/CAFE/WALL 단색 유지)은 단계 ②, 밤 라이팅은 단계 ③**(단계별 PR).

## 8. P2.3 단계② 실내 바닥·벽·가구·장식 — 레시피·★비용 함정 (2026-06-21)

실내 단색 3종(HOUSE/CAFE/WALL)을 도트 타일로, 가구·장식 6종을 `create_map_object`로. `main.gd`의 `_build_tileset`이 단색 fill 대신 16×16 PNG를 SOLID source(id 1)에 blit하고, 가구는 손님·잡귀처럼 `_draw_props()`가 캐릭터 *아래*에 바닥정렬로 그린다(`PROP_LAYOUT` 데이터). 충돌은 WALL 타일만 — 가구는 순수 장식(art 패스라 새 시스템·이동 변화 금지).

### 8.1 ★ 비용 함정 — `create_tiles_pro`는 ~20–40 gen (피하라)
실내 바닥/벽 같은 **단일 면 타일**을 만들 때 `create_tiles_pro`를 먼저 시도했으나 호출 직후 `cost: ~20-40 generations`로 떴다 — §4.4가 금지한 `create_1_direction_object`(트라이얼 몰살)와 같은 등급. **생성 중 삭제는 막혀(이미 과금) 환불 불가.** 16타일(variations 16)을 한 번에 채색하느라 비싸다.
- 함정: cost는 호출 *결과*에만 떠 사전 회피 불가 → **`create_tiles_pro`는 비용을 알고 쓸 때만**(작은 변형 다발이 정말 필요할 때). 단일/소수 타일엔 부적합.
- 부산물: 이미 과금됐으니 그 16타일에서 쓸 것만 골라 건졌다 — `tile_2`=청회 판석(집)·`tile_4`=앰버 판자(카페)·`tile_13`=어두운 벽돌(벽). 16×16 flat(`square_topdown`+`tile_view=top-down`+`depth_ratio=0` → 깊이 없는 정면 타일). 4×4 반복 이음매 점검 후 `assets/tiles/{house_floor,cafe_floor,wall}.png`로 저장.
- 폴백(저비용)으로 같이 띄운 `create_topdown_tileset`(transition_size=0, lineless, 16px) ≈ 2.5 gen는 집·카페 바닥 base 2장을 정상 산출 — **다음에 단일 면 타일이 필요하면 이 경로**(base_tile_id로 추출). 이번엔 tiles_pro가 벽까지 줘서 그걸 썼다.

### 8.2 가구·장식 = `create_map_object` (§6.1 작물과 같은 경로)
- 공통: `view="high top-down"`, `outline="selective outline"`(전경 컨벤션), `shading="basic shading"`, 꼬리 `muted underworld palette, selective dark tonal outline not pure black, 2-3 tone shading`.
- 6종: 카페 카운터·스툴·선반 / 집 침대(32×64) / 소울 등불(영혼불 블루) / 혼령초 화분. 침대만 height 64로 1×2칸, 나머지 32×32.
- 다운로드: §6.2 글루 그대로(`/mcp/map-objects/<id>/download` curl). **32px 원본 → `tools/downscale_prop.py`(PIL NEAREST 2:1)로 16×16(침대 16×32)** 다운스케일 = ADR-0003 16px 격자·픽셀밀도 일관(원본 32px는 격자보다 2배 고밀이라 그대로 그리면 안 됨). 원본은 `*_raw.png`로 백업 커밋(①의 terrain _raw와 같은 결).
- **rate limit:** §6.4대로 4건씩 끊어 던졌다(4 + 2).

### 8.3 통합·검증
- `_build_tileset`: 단색 fill 루프 → `load(SOLID_TEX[id]).get_image()` blit(아틀라스 결·WALL 충돌·칠 순서 불변 → `_paint_grid`·`_set_tile`·세이브 손 안 댐).
- `_draw_props()`: `_draw` 맨 앞에서 호출. 좌석 스툴(y7) 위에 손님 박스가 자연히 덮인다. 배치(직원 y5/카운터 y6/스툴 y7/통로 y8)는 바 레이아웃으로 읽힌다.
- ★**그릴 순서 함정(Godot):** main(Node2D 루트)의 `_draw`는 *부모* 그리기라 **자식 노드(Ground/Field TileMapLayer) 아래에 깔린다** → 가구·손님·밭 커서가 바닥 타일에 가려진다(헤드리스 검증은 `_draw`를 안 불러 못 잡고, *창 띄워야* 드러남 — 잠복했던 손님·커서 가림까지 이번에 표면화). **수정: `ground.z_index = -1`·`field_layer.z_index = -1`** → `_draw` 오버레이가 바닥 위·캐릭터(자식 z0) 아래로 올바르게 낀다. (대안: 타일맵 뒤·캐릭터 앞에 전용 Props 자식 노드. z 한 줄이 더 간단·근본.)
- `tools/map_dump.gd`에 `PROP_LAYOUT` 합성 추가(GPU 없이 가구까지 한 장 확인).
- 검증: 헤드리스 import·부팅(120f) 클린, 회귀 7종(cafe·night·bana·weave·milestone·npc·cafe_margin) PASS, 맵 덤프 배치 육안 확인.

### 8.4 비용·범위
- gen: 가구·장식 6 + 바닥/벽(tiles_pro ~20–40 함정 1회 + 폴백 topdown ~2.5). **교훈: 단일 면 타일은 topdown_tileset base, tiles_pro 금지.**
- 단계 ② 범위 = 실내 표면·가구·장식만. **밤 라이팅(CanvasModulate/Light2D)은 단계 ③.**

### 8.5 ★ 첫 플레이 피드백 반영 (gen 0 — 후처리·연결)
직접 띄워 보니 ㉠ 가구가 안 보임(8.3 그릴 순서), ㉡ 가구·실내가 "너무 탁함"(어두움), ㉢ 캐릭터가 회색 박스. 사용자 요청대로 ㉡㉢ 반영(한글 가독성은 P2.5 유지):
- **밝기 후처리(`tools/brighten.py`, PIL Brightness·Contrast·Color, 알파 보존):** "muted underworld"가 어두운 배경에 묻혀 탁 → 바닥/벽·가구를 자산별 계수로 밝게(집바닥 1.42 / 카페바닥 1.12 이미 밝음 / 벽 1.48 / 가구 1.30–1.35). 바닥/벽 pristine은 `*_raw.png` 백업 후 거기서 파생(재튜닝). 저승 톤은 유지하되 또렷.
- **캐릭터 스프라이트 연결(P2.1 완성):** P2.1은 도트 *에셋*만 만들었고 게임 노드는 그레이박스였다. `char_sprite.gd`(`CharSprite.make`)가 시트(48×48·행=방향·워크N/대기1)를 AnimatedSprite2D로 만들어 player·miho·okja·mel·bana 노드에 붙인다. **콘텐츠가 48×48 안에서 이미 ~16×35px(주변 패딩)이라 네이티브 스케일·발치정렬(offset y −16)이면 16×32 규격과 맞아 다운스케일 불요.** 그레이박스 `_draw`는 스프라이트 있으면 폴백으로 빠진다. 플레이어만 이동 시 워크 애니, 나머지는 down 첫 프레임 정지. `map_dump.gd`가 캐릭터 프레임까지 합성해 한 장 확인.
- 카페 floor 앰버는 여전히 다소 밝음(아늑한 의도) → 거슬리면 P2.7 톤 패스에서 하향.

## 9. 캐스트 비주얼 패스② — v3 도트 재생성 + 대화용 초상화 (2026-06-21)

첫 플레이 피드백 "캐릭터가 이쁘고 또렷하면 좋겠다, 특히 얼굴이 안 보이고 못생김"을 두 갈래로 반영.
얼굴은 본질적으로 16×32 도트의 영역이 아니다(ADR-0003) → **얼굴=초상화, 도트=v3로 또렷하게**.

### 9.1 인게임 도트 = `create_character` **v3 모드**로 재생성 (standard → v3)
- P2.1 도트는 `mode="standard"`(1 gen, soft guidance)라 얼굴·실루엣이 뭉갰다. **v3(2 gen, 8방향, 최고품질)** 로 5종 재생성하니 옷·헤어·소품이 또렷이 읽힌다(옥자 마녀모자+빨간깃털, 바나 투톤+보라로리타, 멜 만주모자+청록치파오+홍염주, 미호 흰털+노란치마, 플레이어 무채 청년).
- 호출: `mode="v3", size=32, view="low top-down", outline="selective outline", detail="high detail"`. **v3는 n_directions/shading/proportions 무시**(항상 8방향). description은 §5.1 원작 외형을 영어로(첨부 일러스트 충실).
- ★ **8방향 → 4방향 추출:** v3는 8방향(south/north/east/west + 대각). CharSprite 규약은 4행(down/up/right/left=4정방향)이라 정방향만 뽑는다.
- ★ **캔버스 56–64px·콘텐츠 ~28–30px:** size=32 요청해도 v3 캔버스가 56–64px로 나오나 **콘텐츠 높이가 ~28–30px**라 16×32 타겟에 거의 일치 → **다운스케일 불요**(v3 선명도 보존). 프레임마다 캔버스 크기가 달라 그대로 합치면 안 되고 **콘텐츠 bbox 기준 발치정렬**해야 한다(§9.3).

### 9.2 워크 애니 = `animate_character` template walk, **4 정방향만**
- `template_animation_id="walk", directions=["south","north","east","west"]` → 방향당 6프레임, **4 gen**(8방향 기본은 8 gen 낭비). 움직이는 player·miho만. 상주 NPC(옥자·바나·멜)는 rotation idle(애니 0).
- §5.3의 v3 커스텀(꼬리 흔들림 억제)은 이번 미호가 "exactly ONE single fox tail"+단순 포즈라 template로도 충분히 안정 → 우선 template, 떨리면 v3 폴백.

### 9.3 ★ 글루 파이프라인 (tools/assemble_char.py — 신규, ADR-0001 허용)
- **다운로드 = 캐릭터 zip 엔드포인트.** `https://api.pixellab.ai/mcp/characters/<id>/download`(Bearer `PIXELLAB_API_KEY`, `curl -L`)가 rotations + animations(`animations/walking/<dir>/frame_00N.png`) 전부 든 zip을 준다. **워크 잡이 끝나기 전엔 HTTP 423** → 완료 폴링 후 받기. (개별 rotation은 backblaze 직링크로도 받힘.)
- **조립(`assemble_char.py <입력dir> <out.png>`):** 입력 `<dir>/*.png`(워크) 또는 `<dir>.png`(idle)를 읽어 4행 시트로. 프레임 48×48, **콘텐츠 발치를 y≈40(FOOT_Y)·가로중앙**(기존 okja 발치 y40과 동일 → char_sprite offset −16과 맞물림). **워크는 방향별 공통 bbox**로 크롭해 사이클 내 상하 흔들림·팔다리 스윙을 보존(프레임별 bbox면 떨림).
- ★ **hole-fill 내장(§5.2):** 경계 flood-fill로 외부투명 vs 갇힌 hole 구분 → hole만 인접 불투명색 dilation. 미호 흰 털 속 구멍을 메운다.
- 임포트 갱신은 §5.4(같은 이름 교체 → `.import`·`.godot/imported/X-*` 삭제 후 `--import`).

### 9.4 대화용 초상화 = 첨부 일러스트 정리 (ADR-0003 "별도 일러스트", 도트 아님)
- 얼굴이 또렷한 곳은 초상화. **첨부 제미나이 일러스트(원작 IP 진실의 원천 §5.1)가 곧 초상화 원본** → 새로 그리지 않고 정리만 한다.
- `tools/make_portrait.py`(PIL, ADR-0001 허용 색보정·크롭): ① 그린스크린 제거+디스필(초록 fringe 억제, 이미 투명이면 건너뜀) → ② 알파 오토크롭 → ③ **얼굴 중심 버스트(상단 ~46%) 정사각 320 캔버스** `assets/portraits/<name>.png` + 전신 `<name>_full.png`. 4종(옥자·미호·바나·멜) 산출, 얼굴 또렷.
- 미리보기: `portrait_preview.tscn`(`bash run_portraits.sh`) — 가짜 대화창에 초상화+이름+대사. 초상화는 일러스트라 `TEXTURE_FILTER_LINEAR`(도트와 달리 보간 허용). **플레이어 초상화는 미제작**(스타듀처럼 자기 얼굴은 대화에 안 띄움 + 무채 컨셉).

### 9.5 비용·범위
- gen: create v3 5종 × 2 = **10** + walk 2종 × 4방향 = **8** ≈ **18 gen**. 초상화·후처리 = gen 0. (잔량 충분, Tier 1.)
- **seam:** 대화/초상화 *시스템*은 아직 없음(P2 후반). 이번엔 초상화 *에셋*과 미리보기 하네스까지. 도트는 게임 노드에 이미 연결돼(§8.5) 즉시 반영.
- PixelLab 캐릭터 ID(재애니/수정용): 옥자 `40b27490` · 미호 `22a62476` · 바나 `4faaf28e` · 멜 `68537d6e` · 플레이어 `6644df31`.

## 10. 캐스트 비주얼 패스③ — "얼굴 또렷" 반복 + 최종 머리확대 후처리 (2026-06-21)

§9 직후 직접 플레이 피드백이 세 번 들어와 방향을 좁혔다. **사용자 1순위 = "캐릭터 얼굴이 도트에서 뚜렷하게 보이는 것"**. 시행착오와 최종 해법을 기록(같은 길 다시 안 파게).

### 10.1 ★ 버린 길(왜 안 됐나)
- **(A) 슬림 재생성**(standard, `proportions=stylized`, `flat shading`, `low detail`, 다운스케일): "너무 흐려지고 얇다"고 반려. flat+low가 v3 선명함을 잃음 → **v3 선명함은 타협 불가**가 확정됨.
- **(B) 큰머리 standard 바블헤드**(`mode=standard`, `proportions` custom `head_size=1.9`, size 48): 얼굴은 확실히 보였으나 **standard라 반려**("v3 무조건"). v3는 `proportions`를 *무시*해서 머리비율을 못 키운다 — standard로 가야 큰머리가 되는데 그게 안 됨.
- 교훈: **v3 유지 + 얼굴 또렷 + 작은 크기**는 픽셀 물리상 동시 만족 불가(low top-down에서 얼굴 ~5px). 셋 중 "v3"와 "얼굴"이 우선 → 크기를 양보.

### 10.2 ✅ 최종 = v3 그대로 + **머리만 후처리 확대** (`tools/enlarge_head.py`, gen 0)
- v3가 머리비율을 못 바꾸니 **생성 후 시트에서 콘텐츠 상단(머리)만 배율 확대**해 다시 얹는다. v3 픽셀을 그대로 키우는 거라 **v3 룩·디자인 유지하며 얼굴 픽셀 수만↑**(요청 "old 이미지에서 얼굴만 좀더 뚜렷" 정확 충족).
- 파이프라인: 프레임마다 콘텐츠 bbox → 상단 `head_ratio`(0.46, ~2.5등신) 밴드를 `factor`(채택 **1.4**)로 **NEAREST** 확대 → 몸 위에 가로중앙·목 2px 겹쳐 재합성 → 48프레임 발치정렬(FOOT_Y 40). 모든 방향·워크 프레임 일괄.
- ★ **확대는 반드시 NEAREST.** 처음 LANCZOS로 했더니 "머리만 흐려 몸과 따로 논다"고 반려 — 보간하면 머리가 부드러워져 또렷한 도트 몸과 결이 안 맞는다. NEAREST면 머리도 몸과 같은 크리스프 도트(픽셀 폭이 1.4배라 살짝 불균일하나 선명·일체감 OK).
- 배율은 **후처리라 재튜닝 1줄**: `enlarge_head.py <_v3raw.png> <out.png> <factor>`. 원본 v3는 `*_v3raw.png`(idle 3종·player_walk)·`miho_walk_multitail_raw.png`로 백업.

### 10.3 캐릭터별 최종 베이스
- 옥자·바나·멜·플레이어 = **§9 1차 v3**(`*_v3raw.png`)에 머리×1.4. (멀티 그대로 OK, 얼굴만 문제였음)
- **미호 = 꼬리 하나 v3로 교체**(원 v3는 흰 털뭉치 여러 개가 날개/다꼬리처럼 보임 → 반려). 재생성 `mode=v3`, description에 **"exactly ONE single fox tail, arms down close to body"** → walk(template, 4정방향) → 머리×1.4. ID `6ccb5389`. (원 멀티테일 `22a62476`는 삭제.)
- **결과 인상 = 약한 바블헤드**(머리 큼). 얼굴 1순위의 의도된 트레이드오프. 거슬리면 factor 1.25로 낮추면 됨.

### 10.4 비용
- gen: 미호 꼬리하나 재생성 v3 2 + walk 4 = **6**. 옥자·바나·멜·플레이어는 §9 자산 재사용(머리확대=gen 0). 슬림/큰머리 시험(버림) 4종은 삭제. 누적 잔량 충분.
- 갱신 캐릭터 ID: 옥자 `40b27490` · **미호 `6ccb5389`(꼬리1)** · 바나 `4faaf28e` · 멜 `68537d6e` · 플레이어 `6644df31`.

### 10.5 ★★ 머리확대도 폐기 → 최종 = **v3 큰 사이즈(size 64) native** (2026-06-21)
머리확대(§10.2)를 게임에서 보니 두 결함: ㉠ **얼굴이 "찰흙"** — 작은 v3 얼굴(native ~6px)을 키운 거라 *큰 뭉갬*일 뿐 또렷·이쁘지 않음(눈 몰림). ㉡ **머리 쏠림** — 콘텐츠 상단 밴드를 자체 가로중심으로 재배치해 비대칭 요소(미호 꼬리·여우불)가 든 밴드가 한쪽으로 쏠림. → **후처리로 얼굴을 만들 수 없다**(없는 픽셀은 못 키움)가 확정.
- **진짜 해법 = 처음부터 큰 native 얼굴.** 미호로 ① v3 size64 ② standard 큰머리 비교 → **v3 size64가 얼굴이 더 이쁘고 깔끔**(큰머리는 앞머리가 얼굴 덮음). 게다가 순수 v3라 "v3 무조건" 충족. ⇒ **5종 전부 `mode=v3, size=64`로 재생성.**
- description에 **"beautiful clean face clearly visible, large symmetric evenly-spaced eyes, hair/hat NOT covering the face"** 박음(사용자 "눈 몰림·얼굴 가림" 반영). size 64 → **캔버스 124–128px·콘텐츠 ~59–66px**(워크 포함 max 66).
- ★ **규격 64프레임→72프레임으로 상향.** 콘텐츠 ~62 + 워크 보브 마진 → `assemble_char.py FRAME=72, FOOT_Y=68` / `char_sprite.gd FRAME=(72,72), FOOT_OFFSET_Y=-32` / `cast_preview.gd FRAME=72, offset.y=-32`. **map_dump.gd는 프레임을 텍스처에서 동적으로 읽어 무수정**(spr.offset+텍스처 크기 사용). **다운스케일·머리확대·hole-fill 후처리 전부 안 씀**(native v3 = 얼굴 선명·도트 크리스프, 갇힌 hole도 v3 size64에선 거의 없음).
- **트레이드오프 = 캐릭터가 큼**(콘텐츠 ~62px ≈ 3.9타일). 사용자가 "얼굴이 1순위, 커져도 OK"로 수용. 더 작게 원하면 `assemble_char.py --targeth N`으로 다운스케일(얼굴 선명도와 trade).
- **`tools/enlarge_head.py`는 폐기**(이력 보존용으로 남김, 최종 파이프라인 미사용). hole-fill(§5.2)도 v3 size64 워크에선 불요.
- **최종 캐릭터 ID:** 옥자 `19ae79e1` · 미호 `c81a1244`(꼬리1) · 바나 `182575e6` · 멜 `fdd01e5d` · 플레이어 `b10faf8a`. (이전 시험본은 전부 삭제.)
- 검증: 헤드리스 import 클린, map_dump·워크 시트 육안 — 5종 얼굴 또렷·이쁨, 미호 꼬리 1, 워크 4방향 정상, 쏠림 없음.

### 10.6 ★ 환경 2배 스케일업 (ADR-0012) — 큰 캐릭터에 맵·시점 맞추기 (2026-06-21)
v3 size64 캐릭터(~62px)가 16px 타일·가구·320×180 화면 대비 과하게 컸다("맵·시점도 넓어져야"). **C안 = 환경을 전부 2배로** 올려 비율 일치(상세·근거 = ADR-0012).
- **타일 그리드 불변**(좌석·NPC·동선 좌표·회귀 8종 불변). 바뀐 건 "타일당 픽셀"뿐: `TILE 16→32`(월드 좌표) + `TILE_ART=16`(타일셋/오버레이 *생성*은 16px 유지). `_build_tileset`/`_build_field_tileset`은 TILE_ART로.
- **렌더 ×2:** `Ground`/`Field` TileMapLayer `scale=(2,2)`(WALL 충돌 폴리곤도 자동 2배) · `_draw_props`는 `draw_texture_rect`로 2배 · 내부해상도 `640×360`(§project.godot) + UI는 `CanvasLayer scale=(2,2)`로 320×180 레이아웃 그대로(위치·크기 불변).
- **파생 2배:** 플레이어 콜리전 28×20(player.tscn) · SPEED 160(player.gd) · LAMP_TEXTURE_SCALE 3.2(lighting.gd) · map_dump.gd 미리보기 ×2.
- 결과: 캐릭터=약 2타일, 가구·타일과 비율 일치, 640×360에서 20×11타일 보여 팔로우 카메라 복원. **회귀 8종 PASS·부팅 클린.**
- 신규 환경 아트는 계속 16px로 만들면 됨(렌더만 ×2 — ADR-0003 작업량 1/4 이점 보존).

### 10.7 캐릭터별 플레이 피드백 보정 (2026-06-21)
2배 적용 후 플레이 피드백 — 캐릭터별 미세 조정:
- **멜**: 만주 소매가 벌어져 덩치 커 보임 → v3 재생성에 "arms held straight down at the sides, sleeves NOT spread out wide" → 폭 34→31. ID `72bdbb27`.
- **바나**: 풍성한 머리+넓은 드레스로 큼 → 1차로 `--targeth 54` 축소했으나 **LANCZOS 다운스케일이 다른 캐릭터(native)보다 흐려 보임**(피드백). → **슬림하게 v3 재생성**("hair sleek not voluminous, slim narrow dress")해 native 폭 38(다른 캐릭터급)로 줄여 **축소 없이 선명** 복원. + **빨간 눈 → 정상 눈**("normal dark eyes NOT red"). ID `ca97df46`.
- **옥자**: 안경 요청이 두 번 바뀜 — "반달 안경"(도트 스케일 표현 불가, 얼굴 14px) → 최종 **"안경 제거"**. v3 재생성 "NO glasses, bare eyes". ID `05f6603d`. **교훈: 안경류 디테일은 도트 스프라이트에 안 박힘 — 대화 초상화 영역(ADR-0003).**
- 미호·플레이어는 유지.
- (이 단계 ID는 §10.8에서 전부 교체됨 — 체형 통일 위해 standard 재생성.)

### 10.8 ★★ 체형 통일 → v3 폐기, **standard 고정 비율**로 5종 재생성 (2026-06-21)
"메인 캐릭터들 체형·비율이 안 맞는다, 모두 멜급 체형으로 통일" 피드백. 진단: **v3는 캐릭터마다 머리:몸 비율이 제각각**(플레이어=작은 머리/realistic, 바나·옥자=큰 머리). v3는 `proportions`를 무시해 통일 불가 — **이게 안 맞던 근본 원인**.
- **해법 = `mode=standard` + `proportions` custom 고정값으로 5종 전부 재생성.** standard는 proportions를 따르므로 **모두 같은 머리:몸:팔:다리 비율** → 체형 통일. `detail="high detail"`이라 흐리지 않음(예전 반려 "슬림"은 flat+low-detail이었음).
- 공통 파라미터: `mode=standard, n_directions=4, size=56, view=low top-down, outline=selective, shading=basic, detail=high`, `proportions={"type":"custom","head_size":1.4,"arms_length":0.75,"legs_length":0.95,"shoulder_width":0.72,"hip_width":0.75}`(슬림 치비·팔 내림). description엔 §10.7 정체성·수정(옥자 안경X·바나 정상눈) + "slim chibi build, arms straight down, beautiful clean face, large evenly-spaced eyes" 공통.
- **규격 frame 72→80:** standard size56 콘텐츠 ~58-70px(옥자 모자로 큼). `assemble FRAME=80 FOOT_Y=76` / `char_sprite FRAME=(80,80) FOOT_OFFSET_Y=-36` / `cast_preview FRAME=80 offset -36`. native 조립(다운스케일 없음=선명). map_dump 무수정(프레임 동적).
- **운영:** size56 standard 5종을 한 번에 던지니 2건이 `Generation failed (heavy load)` → 재시도로 통과(§6.4 결). 캐릭터 standard도 가끔 서버측 실패 — 폴링으로 failed 확인·재던지기.
- 검증: 5종 발치정렬 비교 — 머리:몸 비율 통일, 얼굴 또렷, 미호 꼬리1·정상눈 바나·안경X 옥자, 워크 4방향 정상. 헤드리스 import·부팅 클린, **회귀 8종 영향 없음**(좌표 불변).
- 비율 바꾸려면 `proportions.head_size` 등 조정해 재생성(통일하려면 5종 동일값).

### 10.9 플레이어·바나 개별 보정 (2026-06-21)
체형 통일 후 둘만 추가 보정(나머지는 통일 비율 유지):
- **플레이어**: "너무 마르고 목이 길어 얼굴이 떠다님" → proportions를 단단하게(남성 빌드): `head_size 1.3, shoulder_width 0.95, hip_width 0.82, arms 0.85, legs 0.9` + desc "sturdy, broad shoulders, short neck, head on shoulders". 머리가 어깨에 붙어 해결. ID `75197b73`(+walk). **통일 5종보다 약간 넓은 남성 빌드 — 의도된 예외.**
- **바나**: 통일 재생성이 "뱀파이어 룩 없어지고 평범"해짐 → desc에 "gothic vampire, purple and black gothic lolita, high collar, fangs, pale skin, bat motif, **normal dark eyes NOT red**"(눈은 이전 요청대로 정상색 유지). 통일 비율 그대로. ID `6f3a020f`. (red gown으로 드리프트한 시도는 폐기.)
- **운영:** standard size56을 연달아 던지면 `heavy load` 실패 잦음 — 바나만 2회 재시도. 폴링 watcher가 실패분(south.png 안 뜸)을 영영 기다리니 실패 확인 시 새 watcher로 교체.
- (이 단계 ID는 §10.10에서 옥자·바나·플레이어가 재교체됨.)

### 10.10 바나 옷(참조)·옥자 코·플레이어 귀엽게 (2026-06-21)
- **바나**: 원작 참조(보라/검정 고딕로리타 풀세트 — 라벤더 프릴 머리띠·레이스·층층 프릴스커트·다홍 레이스업 부츠·투톤 머리)로 옷 맞춤. desc에 그대로 박음. 눈은 정상색 유지. 비율 통일 그대로. ID `671b1c80`.
- **옥자**: "코가 1자(세로 선)" → desc "small soft cute nose, NO long vertical line nose". 다른 캐릭터처럼 자연스러운 코. ID `9bd60dd6`.
- **플레이어**: "얼굴·머리 못남" → desc "cute handsome, soft fluffy neat hair cute hairstyle, cute friendly face". 단단한 남성 비율(§10.9)은 유지. ID `8ddec0f6`(+walk).
- (이 단계 ID는 §10.11에서 옥자·멜·바나·플레이어 재교체.)

### 10.11 볼·얼굴크기·옷 추가 보정 (2026-06-21)
- **옥자**: "볼 발그레 부자연스러움" → desc "no blush"가 안 먹혀(standard가 분홍 볼을 계속 baked) **후처리로 제거**: `tools`에 인라인 — 얼굴 밴드(콘텐츠 20-52%)에서 연분홍 볼 픽셀(R>205·R−G>55·B>135)을 피부색(245,200,190)으로 치환(입술·피부 보존). ID `ac4656a2`. **교훈: 볼홍조·안경·코 같은 face 디테일은 standard가 baked — 프롬프트보다 후처리가 확실.**
- **멜**: "얼굴 살짝 작게" → `proportions.head_size 1.4→1.25`. ID `c64b6f3c`.
- **바나**: "무릎치마·보라검정" → desc "dark purple and black color scheme, knee-length skirt". ID `b95aca25`.
- **플레이어**: "얼굴 더 둥글게·옷 검정" → desc "round soft cute face, all black outfit(black jacket)", head_size 1.3→1.35. ID `60e61004`(+walk).
- **운영:** standard `heavy load` 실패 잦음 — 플레이어 2회 재시도. 4건씩 끊어 던지는 게 안전(§6.4).
- (옥자·바나는 §10.12에서 재교체.)

### 10.12 옥자 눈·바나 그림체·★미호 서있는 포즈 (2026-06-21)
- **미호**: "걷는 포즈(서있지 않음)" — 원인: miho는 정지 station NPC인데 **워크 시트를 쓰면 첫 프레임이 스트라이드**(template walk frame0=중간 보폭)라 걷는 듯 보임. miho는 텔레포트만 하고 걷지 않음 → **rotation(서있는) 4×1 idle 시트로 교체**(okja/bana/mel과 동일, gen 0). `miho_walk.png`를 1프레임/방향으로 덮어 char_sprite가 1열→정지. **교훈: 정지 NPC엔 워크 시트 frame0이 아니라 rotation idle을 써야 선다.**
- **옥자**: 눈이 어둡게(텅 빈 듯) → desc "large clear normal eyes with visible iris, NOT dark hollow". 정상 눈. + 볼 후처리 재적용. ID `c78cbf52`.
- **바나**: "그림체가 또 다름"(밝은 머리·큰 모에 눈) → desc "same simple clean chibi style, normal-sized eyes not oversized, blonde with black streaks not platinum" + 볼 후처리. 다소 개선(다만 standard 변동성으로 완전 일치는 어려움). ID `2d8ed731`.
- (바나는 §10.13에서 얼굴 재보정.)

### 10.13 바나 얼굴 귀엽게 (2026-06-21)
"바나 옷 스타일은 좋은데 얼굴(입술·크기)이 부자연스러움 → 더 귀엽게". 옷 유지, desc에 "VERY cute pretty natural face, small simple cute mouth with tiny lips (NOT large lips), soft gentle expression, normal-sized eyes" 강조 → 큰 입술 사라지고 귀여운 자연 얼굴. 머리도 따뜻한 금발로 톤 근접. + 볼 후처리. ID `90daee91`.
- **최종 캐릭터 ID:** 옥자 `c78cbf52` · 미호 `c938bf47`(idle) · 바나 `90daee91` · 멜 `c64b6f3c` · 플레이어 `60e61004`.

## 11. 환경 선명화 — 32px native 상향 (ADR-0013, 2026-06-21)

ADR-0012로 캐릭터를 native 고해상도로 만든 뒤 직접 플레이에서 **"환경(타일·가구·밭)이 캐릭터 대비 흐리다"**가 확인됐다(16px를 ×2 업스케일했기 때문). 환경을 전부 **32px native**로 올려 픽셀밀도를 캐릭터·작물과 맞춘다(상세·근거 = ADR-0013).

### 11.1 지형 = `create_topdown_tileset tile_size=32` 재생성 + 컨버터 재실행
- §7.1 레시피 그대로, `tile_size={"width":32,"height":32}`만 추가. 3세트(풀↔길·밭↔풀·길↔밭), base_tile_id 체이닝으로 이음매 일치(풀 base를 밭·길 세트에 upper/lower_base로 물림).
- 다운로드: `curl -sL -H "Authorization: Bearer $PIXELLAB_API_KEY" .../tilesets/<id>/{metadata,image}` (302 redirect → `-L` 필수). 컨버터 명명 `*_metadata.json`/`*_image.png`로.
- 풀 채도 하향(§7.3 `desaturate_grass.py`)은 **`_raw` 백업이 16px 잔존이면 먼저 지우고** 재실행(스크립트가 `_raw`에서 출발하므로, 안 지우면 새 32px가 옛 16px로 되돌아감).
- 컨버터(`pixellab_tileset_converter.gd`)는 tile_size를 metadata에서 읽어 **32px도 그대로 처리**(코드 무수정). terrain id 순서는 인자 순서로 고정(0=길·1=풀·2=흙, main.gd `TILE_TERRAIN`과 일치).

### 11.2 실내 단일 면 타일 = 32px topdown base 추출 (`tools/extract_base_tile.py` 신규)
- §8.1 교훈("단일 면 타일은 tiles_pro 금지, topdown base 경로") 이행. `create_topdown_tileset`(32px, `view=high top-down`, transition_size=0)으로 집바닥↔카페바닥 한 쌍·벽↔풀 한 쌍을 생성.
- 추출기(신규 글루): metadata의 `tiles[].corners`(NE/NW/SE/SW)가 **모두 lower(또는 모두 upper)인 base 타일**을 찾아 `bounding_box`로 아틀라스에서 32×32 크롭. `extract_base_tile.py <meta> <img> <lower|upper> <out>`.
- 밝기 후처리(§8.5 `brighten.py`): 집바닥 1.42·카페바닥 1.12·벽 1.48(pristine은 `*_raw.png` 백업 후 파생).

### 11.3 가구 = 32px `_raw` native 직접 사용 (gen 0)
- §8.2가 `downscale_prop.py`로 32px→16px 줄였던 것을 **폐기**. 기존 `*_raw.png`(32px pristine)에 `brighten.py 1.32`만 걸어 `props/*.png`(32px)로. `_draw_props`의 `×2` 제거 → native 1:1(스툴 32×32=1칸, 침대 32×64=1×2칸).

### 11.4 작물 연결 = 그레이박스 점 → P2.2 스프라이트 (T2.3 seam 해소)
- P2.2 작물(32px, 혼령초·피안화·영혼 호박 각 seed/sprout/mature)을 밭에 연결. `field.growth_stage(t)` 0/1/2 → 같은 인덱스 프레임(★seam 깔끔: 밭 외형 3단계 = 아트 3프레임, crops.gd `stages`(혼2/피3/호4)는 시각에 안 씀).
- `main.CROP_SPRITES`(id별 3프레임 preload) + `_draw_crops`가 `farm.planted_tiles()`를 칸(32×32)에 바닥정렬로 그린다(가구·손님과 같은 결, 캐릭터 아래·흙 오버레이 위). `field.gd`에 `planted_tiles()` 추가. `_on_tile_changed`에 `queue_redraw`(작물은 _draw에서 그리므로 상태 변화 시 재그림).
- 밭 오버레이 타일(`_build_field_tileset`)은 **경작 고랑 DRY/WET 흙 톤만** 남기고 그레이박스 점(`AP_DOT`/SPROUT·MATURE 색) 제거. 외형단계 인덱스는 `_overlay_index` 호환 위해 유지.

### 11.5 코드·검증
- `main.gd`: `TILE_ART 16→32`, `_draw_props ×2` 제거, `_build_field_tileset` 점 제거, `_draw_crops` 추가. `main.tscn`: Ground/Field `scale=(2,2)` 제거(UI CanvasLayer scale 유지). `tools/map_dump.gd`: 타일·가구 ×2 리사이즈 제거 + 작물 미리보기.
- 비용: 지형 5 gen + 실내 2 gen ≈ 7 gen(가구·작물 재사용 = gen 0). 잔량 충분(Tier 1).
- 검증: 회귀 8종(cafe·cafe_margin·night_bar·weave·milestone·npc_station·lighting·bana) PASS, 헤드리스 부팅·임포트 클린, map_dump 육안(환경 선명·작물 3종×3단계 연결) 확인.

## 12. P2.5 UI 최소 패스 — 레시피·발견 (2026-06-21)

> 그레이박스 기능형 UI를 에셋으로. **단계별 PR**: ①한글 고해상도 폰트 → ②하트 스프라이트+패널 스킨 → ③대화 프레임 패널+작물 아이콘. 폰트는 PixelLab이 아니라 **OFL 비트맵 폰트 도입**(라이선스 = licensing-checklist 폰트 항목).

### 12.1 단계① 한글 고해상도 폰트 (gen 0 — 폰트 도입+레이어 재구성)
- **문제(P2.3 §8.5에서 P2.5로 미뤄둠):** Godot 기본 폰트(벡터·안티엘리어싱)를 UI CanvasLayer **scale 2x**(논리 320×180)에 올리니 한글 음절이 8px로 렌더된 뒤 ×2 확대 → 뭉개져 가독 불가.
- **해법:** ① **Neo둥근모(NeoDunggeunmo v1.601, SIL OFL 1.1)** 도입 — 한국어 16px 설계 비트맵 폰트. `assets/fonts/neodgm.ttf` + 라이선스 사본 동거. ② UI CanvasLayer **scale 2→1**(논리 640×360)로 내려 폰트를 **16px 네이티브 1:1 렌더**(설계 크기 = 또렷). 모든 자식 offset 2배·`font_size=8` 오버라이드 제거.
- **전역 적용 = 프로젝트 기본 테마.** CanvasLayer는 Control이 아니라 `theme`를 못 가진다 → `ui_theme.tres`(`default_font=neodgm`, `default_font_size=16`)를 만들어 `project.godot [gui] theme/custom`으로 전역 기본 테마 지정. 모든 Control에 자동 적용(per-label 오버라이드 불필요).
- **★ stretch=viewport 주의:** 윈도우 stretch가 `viewport`라 CanvasLayer scale과 무관하게 전체가 640×360 내부해상도로 렌더된 뒤 정수배 확대된다. 그래서 폰트는 **반드시 16px 네이티브로 렌더**해야(scale 1) 640×360 격자에 1:1로 떨어진다. scale 2 + font_size 16이면 32px로 두 배 커져 라벨 박스 넘침, scale 2 + font_size 8이면 설계 미달로 뭉갬 → **scale 1 + font_size 16만 정답**.
- 검증: 회귀 8종 PASS·임포트·부팅(120f) 클린 + **`tools/hud_dump.gd`(신규 글루, light_dump 결)로 실제 GPU 렌더 HUD 육안**(헤드리스론 폰트 안 보임) — 한글 또렷 확인.

### 12.2 단계② 하트 스프라이트 (PixelLab 1 gen + 파생 1)
- **채운 하트 = PixelLab `create_map_object` 1 gen** — "plump rounded heart, glowing warm ember orange fading to soft pink, foxfire inner glow"(여우불 톤), 32×32, `single color outline`(전경 규칙)·`basic shading`. `assets/ui/heart_full_32.png`.
- **빈 하트 = 채운 하트에서 파생(gen 0, `tools/make_hearts.py`)** — 둘을 따로 생성하면 실루엣이 어긋난다 → 채운 하트 알파를 16px에서 **1px 침식(MinFilter3)**해 외곽 링만 남기고(차분한 회보라 `#968c9b`) 속은 비운다. ★32px에서 침식 후 다운스케일하면 링이 얇아져 끊김 → **표시 크기(16px)에서 직접 1px 링**. 채운 하트도 같은 스크립트가 16px nearest 다운스케일.
- **HUD 연결 = 재사용 `HeartBar`(HBoxContainer)** — 이름 라벨 + 채운/빈 TextureRect 5개 + 카운트. `affinity.heart_bar()`의 ♥♡ 폰트 글리프를 스프라이트로 대체. 미호·멜·바나가 같은 씬 인스턴스 3개(affinity.gd 결), `main`이 `render(이름, 채운수, 총수)`만 호출. 슬롯 생성은 **지연 멱등 `_build()`**(헤드리스 `_initialize`에선 `_ready` 자동 호출 안 됨 → render·테스트가 먼저 와도 안전).
- 검증: `heart_bar_test`(슬롯5·채움3/빈2·♡0·만렙·인덱스 초과 포화 8단언) PASS·회귀 8종 PASS·부팅 클린·hud_dump 육안(미호 ♥♥♥♡♡·멜 ♥♥♡♡♡·바나 ♥♡♡♡♡).

### 12.3 단계③ 통일 패널 스킨 + 대화 프레임 (PixelLab 1 gen + 후처리)
- **패널 프레임 = PixelLab `create_map_object` 1 gen** — "rectangular UI dialogue box border, dark charcoal panel, warm amber trim + corner accents, underworld cafe window", 64×64·flat shading·low detail. 속 빈 프레임을 요청했으나 **채워진 라운드 패널 + 앰버 코너**가 나왔는데, 9-slice엔 오히려 적합(가운데 균일 → 타일링 깔끔). `assets/ui/panel_frame_raw.png`.
- **후처리(`tools/make_panel.py`, gen 0):** ① 투명 여백 크롭(bbox) ② 가운데 대각 광택을 균일 어둠(`#2c2c32`)으로 평탄화 — 9-slice가 가운데를 늘릴 때 광택 왜곡 방지. 바깥 9px 링(둥근 모서리+앰버 코너+곧은 테두리)은 PixelLab 출력 보존. 결과 46×47.
- **통일 적용 = 테마 Panel StyleBox.** `ui_theme.tres`에 `StyleBoxTexture`(texture_margin 9 = 9-slice 고정 테두리)를 `Panel/styles/panel`로 등록 → 모든 Panel 노드가 자동으로 같은 스킨. main.tscn의 팝업 4종(Shop·Dialogue·Milestone·CafeSummary)을 **ColorRect→Panel 전환**(코드가 `.color` 안 만지고 `.visible`만 써 안전, `@onready` 타입만 갱신). **EndingPanel·Fade는 전체화면 솔리드라 ColorRect 유지**(프레임 제외).
- **★ 9-slice 핵심:** 한 텍스처가 대화창(608×140)·출하대(400×176)·정산(360×104) 등 제각각 크기에서 코너는 고정·테두리는 늘어나·가운데는 채워진다 → 패널마다 따로 안 만들고 하나로 통일. 대화 프레임도 같은 스킨이 자동 적용(초상화는 P2.4 슬롯 그대로).
- 검증: 회귀 9종(+heart_bar) PASS·부팅 클린·hud_dump 육안(대화창·출하대 두 크기에서 앰버 코너 또렷·본문 채움).
